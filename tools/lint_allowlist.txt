# Allowlist for tools/lint_n8n_workflows.py
# Format per line: RULE|workflow_id|node_name
# Blank lines and lines starting with # are ignored.
# Only add entries after confirming the finding is intentional and documented.

# 2026-04-21 — audit H-2 remediation (docs/SECURITY_AUDIT_2026_04_21.md)
#
# Non-VG workflows sharing the n8n instance. Out of scope for VG audit; owning
# teams must add headerAuth themselves. Re-review quarterly.
AUTH-01|C9dD1Oj6QSvTMX3M|NewsPipeline_Trigger_v3
AUTH-01|Zm7g02zpbt8KiTpI|Specialist Approves

# (Linter stale-cache allowlist entries for OYahvKcydMrUxK8j + EKrWrvJZOoWKL3pl
#  removed 2026-04-21 after the linter was upgraded to join
#  workflow_published_version -> workflow_history for authoritative nodes JSON.)

# 2026-04-29 — JSON-DRIFT-01 latent findings (TODO follow-up).
#
# Pre-existing $json drift in nodes downstream of HTTP Request nodes. Discovered
# by the JSON-DRIFT-01 rule itself. Most are 'Log X' nodes where the impact is
# stale/null project_id in production_log entries (non-blocking). 'Trigger
# Caption Burn' references topic_title/drive_folder_id from the production_log
# response and has been working coincidentally (or with empty fallbacks) — not
# blocking topic 1, 4 progression, but should be fixed in a follow-up sweep
# now that topic 4 is in flight. Gate is allowlisted to keep CI green; do NOT
# add new drift bugs — fix new findings at the source.
JSON-DRIFT-01|Exm836gCGtxNKOeD|Log Assembly Fired
JSON-DRIFT-01|Fhdy66BLRh7rAwTi|Log Concat Failure
JSON-DRIFT-01|Fhdy66BLRh7rAwTi|Log Normalize Failure
JSON-DRIFT-01|Fhdy66BLRh7rAwTi|Trigger Caption Burn
JSON-DRIFT-01|rHQa9gThXQleyStj|Log I2V Started
JSON-DRIFT-01|8KW1hiRklamduMzO|Log Failed
JSON-DRIFT-01|uAlOrkJFjkkXrw6t|Log Alert (Stall)
JSON-DRIFT-01|KQDyQt5PV8uqCrXM|Log T2V Started
JSON-DRIFT-01|4L2j3aU2WGnfcvvj|Log TTS Started
