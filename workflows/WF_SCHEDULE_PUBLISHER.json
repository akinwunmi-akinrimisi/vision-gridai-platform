{
  "updatedAt": "2026-04-26T03:08:12.733Z",
  "createdAt": "2026-03-28T19:02:31.897Z",
  "id": "XQc8xmZo9enid0CO",
  "name": "WF_SCHEDULE_PUBLISHER",
  "description": null,
  "active": true,
  "isArchived": false,
  "nodes": [
    {
      "id": "trigger-1",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        250,
        300
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 15
            }
          ]
        }
      }
    },
    {
      "id": "code-1",
      "name": "Check Due Posts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        300
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Check for scheduled posts that are due for publishing.\n// Rewritten 2026-04-26 — pre-existing bug used this.helpers.httpRequest +\n// process.env, both broken under n8n 2.8.4 task runner (543 consecutive\n// failures since 2026-04-20). Now uses fetch() + $env per platform memory.\n\nconst SB = $env.SUPABASE_URL;\nconst ANON = $env.SUPABASE_ANON_KEY;\nconst SRK = $env.SUPABASE_SERVICE_ROLE_KEY;\nconst sbHeaders = (extra = {}) => ({\n  apikey: ANON,\n  Authorization: `Bearer ${SRK}`,\n  'Content-Type': 'application/json',\n  ...extra\n});\n\nconst now = new Date().toISOString();\n\nconst dueRes = await fetch(\n  `${SB}/rest/v1/scheduled_posts?status=eq.scheduled&scheduled_at=lte.${now}&select=*&order=scheduled_at.asc&limit=10`,\n  { method: 'GET', headers: sbHeaders() }\n);\nif (!dueRes.ok) {\n  const txt = await dueRes.text();\n  throw new Error(`Fetch due posts failed: HTTP ${dueRes.status}: ${txt.slice(0, 200)}`);\n}\nconst duePosts = await dueRes.json();\n\nif (!Array.isArray(duePosts) || duePosts.length === 0) {\n  return [{ json: { published: 0, message: 'No posts due' } }];\n}\n\nconst results = [];\n\nfor (const post of duePosts) {\n  // Mark as publishing\n  try {\n    await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n      method: 'PATCH',\n      headers: sbHeaders({ Prefer: 'return=minimal' }),\n      body: JSON.stringify({ status: 'publishing' })\n    });\n  } catch (_) { /* non-fatal — proceed to publish attempt */ }\n\n  try {\n    // Platform-specific publish logic stub. YouTube uploads are handled by\n    // WF_YOUTUBE_UPLOAD; TikTok/Instagram integration is future work. For\n    // now this just marks the row as published so the queue advances.\n    const patchRes = await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n      method: 'PATCH',\n      headers: sbHeaders({ Prefer: 'return=minimal' }),\n      body: JSON.stringify({ status: 'published', published_at: new Date().toISOString() })\n    });\n    if (!patchRes.ok) {\n      const txt = await patchRes.text();\n      throw new Error(`PATCH failed: HTTP ${patchRes.status}: ${txt.slice(0, 200)}`);\n    }\n    results.push({ id: post.id, platform: post.platform, status: 'published' });\n  } catch (e) {\n    const msg = (e && e.message) ? e.message : String(e);\n    try {\n      await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n        method: 'PATCH',\n        headers: sbHeaders({ Prefer: 'return=minimal' }),\n        body: JSON.stringify({ status: 'failed', error_message: msg.slice(0, 500) })\n      });\n    } catch (_) { /* swallow secondary failure */ }\n    results.push({ id: post.id, platform: post.platform, status: 'failed', error: msg });\n  }\n}\n\nreturn [{\n  json: {\n    published: results.filter(r => r.status === 'published').length,\n    failed: results.filter(r => r.status === 'failed').length,\n    results\n  }\n}];"
      }
    }
  ],
  "connections": {
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Check Due Posts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "executionTimeout": 300,
    "callerPolicy": "workflowsFromSameOwner",
    "availableInMCP": false
  },
  "staticData": {
    "node:Schedule Trigger": {
      "recurrenceRules": []
    }
  },
  "meta": null,
  "pinData": null,
  "versionId": "b37741c1-6720-4c57-af0a-23667ebac4cb",
  "activeVersionId": "b37741c1-6720-4c57-af0a-23667ebac4cb",
  "versionCounter": 51,
  "triggerCount": 1,
  "shared": [
    {
      "updatedAt": "2026-03-28T19:02:31.899Z",
      "createdAt": "2026-03-28T19:02:31.899Z",
      "role": "workflow:owner",
      "workflowId": "XQc8xmZo9enid0CO",
      "projectId": "ccWn1Q4giuKzXIbJ",
      "project": {
        "updatedAt": "2026-01-26T11:09:31.107Z",
        "createdAt": "2026-01-26T03:51:33.410Z",
        "id": "ccWn1Q4giuKzXIbJ",
        "name": "Akinwunmi Akinrimisi <akinolacrypto@gmail.com>",
        "type": "personal",
        "icon": null,
        "description": null,
        "creatorId": "ab010feb-355c-4130-b354-4e929908258e"
      }
    }
  ],
  "tags": [],
  "activeVersion": {
    "updatedAt": "2026-04-26T03:08:12.734Z",
    "createdAt": "2026-04-26T03:08:12.734Z",
    "versionId": "b37741c1-6720-4c57-af0a-23667ebac4cb",
    "workflowId": "XQc8xmZo9enid0CO",
    "nodes": [
      {
        "id": "trigger-1",
        "name": "Schedule Trigger",
        "type": "n8n-nodes-base.scheduleTrigger",
        "typeVersion": 1.2,
        "position": [
          250,
          300
        ],
        "parameters": {
          "rule": {
            "interval": [
              {
                "field": "minutes",
                "minutesInterval": 15
              }
            ]
          }
        }
      },
      {
        "id": "code-1",
        "name": "Check Due Posts",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          500,
          300
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "// Check for scheduled posts that are due for publishing.\n// Rewritten 2026-04-26 — pre-existing bug used this.helpers.httpRequest +\n// process.env, both broken under n8n 2.8.4 task runner (543 consecutive\n// failures since 2026-04-20). Now uses fetch() + $env per platform memory.\n\nconst SB = $env.SUPABASE_URL;\nconst ANON = $env.SUPABASE_ANON_KEY;\nconst SRK = $env.SUPABASE_SERVICE_ROLE_KEY;\nconst sbHeaders = (extra = {}) => ({\n  apikey: ANON,\n  Authorization: `Bearer ${SRK}`,\n  'Content-Type': 'application/json',\n  ...extra\n});\n\nconst now = new Date().toISOString();\n\nconst dueRes = await fetch(\n  `${SB}/rest/v1/scheduled_posts?status=eq.scheduled&scheduled_at=lte.${now}&select=*&order=scheduled_at.asc&limit=10`,\n  { method: 'GET', headers: sbHeaders() }\n);\nif (!dueRes.ok) {\n  const txt = await dueRes.text();\n  throw new Error(`Fetch due posts failed: HTTP ${dueRes.status}: ${txt.slice(0, 200)}`);\n}\nconst duePosts = await dueRes.json();\n\nif (!Array.isArray(duePosts) || duePosts.length === 0) {\n  return [{ json: { published: 0, message: 'No posts due' } }];\n}\n\nconst results = [];\n\nfor (const post of duePosts) {\n  // Mark as publishing\n  try {\n    await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n      method: 'PATCH',\n      headers: sbHeaders({ Prefer: 'return=minimal' }),\n      body: JSON.stringify({ status: 'publishing' })\n    });\n  } catch (_) { /* non-fatal — proceed to publish attempt */ }\n\n  try {\n    // Platform-specific publish logic stub. YouTube uploads are handled by\n    // WF_YOUTUBE_UPLOAD; TikTok/Instagram integration is future work. For\n    // now this just marks the row as published so the queue advances.\n    const patchRes = await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n      method: 'PATCH',\n      headers: sbHeaders({ Prefer: 'return=minimal' }),\n      body: JSON.stringify({ status: 'published', published_at: new Date().toISOString() })\n    });\n    if (!patchRes.ok) {\n      const txt = await patchRes.text();\n      throw new Error(`PATCH failed: HTTP ${patchRes.status}: ${txt.slice(0, 200)}`);\n    }\n    results.push({ id: post.id, platform: post.platform, status: 'published' });\n  } catch (e) {\n    const msg = (e && e.message) ? e.message : String(e);\n    try {\n      await fetch(`${SB}/rest/v1/scheduled_posts?id=eq.${post.id}`, {\n        method: 'PATCH',\n        headers: sbHeaders({ Prefer: 'return=minimal' }),\n        body: JSON.stringify({ status: 'failed', error_message: msg.slice(0, 500) })\n      });\n    } catch (_) { /* swallow secondary failure */ }\n    results.push({ id: post.id, platform: post.platform, status: 'failed', error: msg });\n  }\n}\n\nreturn [{\n  json: {\n    published: results.filter(r => r.status === 'published').length,\n    failed: results.filter(r => r.status === 'failed').length,\n    results\n  }\n}];"
        }
      }
    ],
    "connections": {
      "Schedule Trigger": {
        "main": [
          [
            {
              "node": "Check Due Posts",
              "type": "main",
              "index": 0
            }
          ]
        ]
      }
    },
    "authors": "Akinwunmi Akinrimisi",
    "name": null,
    "description": null,
    "autosaved": false,
    "workflowPublishHistory": [
      {
        "createdAt": "2026-04-26T03:08:12.766Z",
        "id": 3612,
        "workflowId": "XQc8xmZo9enid0CO",
        "versionId": "b37741c1-6720-4c57-af0a-23667ebac4cb",
        "event": "activated",
        "userId": "ab010feb-355c-4130-b354-4e929908258e"
      },
      {
        "createdAt": "2026-04-26T03:08:12.756Z",
        "id": 3611,
        "workflowId": "XQc8xmZo9enid0CO",
        "versionId": "b37741c1-6720-4c57-af0a-23667ebac4cb",
        "event": "deactivated",
        "userId": "ab010feb-355c-4130-b354-4e929908258e"
      }
    ]
  }
}
