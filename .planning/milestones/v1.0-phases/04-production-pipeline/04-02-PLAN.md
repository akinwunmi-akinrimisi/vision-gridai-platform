---
phase: 04-production-pipeline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - dashboard/src/components/production/HeroCard.jsx
  - dashboard/src/components/production/DotGrid.jsx
  - dashboard/src/components/production/QueueList.jsx
  - dashboard/src/components/production/FailedScenes.jsx
  - dashboard/src/components/production/ActivityLog.jsx
  - dashboard/src/components/production/CostEstimateDialog.jsx
  - dashboard/src/components/production/SupervisorAlert.jsx
  - dashboard/src/pages/ProductionMonitor.jsx
autonomous: true
requirements: [PROD-13]

must_haves:
  truths:
    - "Production Monitor shows empty state with approved topic count and Start Production CTA when no topic is producing"
    - "Active topic renders as hero card with stage chips, elapsed time, ETA, and live cost"
    - "172-dot grid grouped by chapter shows scene-level progress colored by furthest completed stage"
    - "Failed scenes section shows retry, edit-and-retry, skip actions per scene and batch"
    - "Queue list shows upcoming topics with remove buttons"
    - "Supervisor alert banner appears when supervisor_alerted=true"
  artifacts:
    - path: "dashboard/src/pages/ProductionMonitor.jsx"
      provides: "Full production monitor page"
      min_lines: 80
    - path: "dashboard/src/components/production/HeroCard.jsx"
      provides: "Active topic hero card with stage chips"
      min_lines: 60
    - path: "dashboard/src/components/production/DotGrid.jsx"
      provides: "Scene progress dot grid grouped by chapter"
      min_lines: 50
    - path: "dashboard/src/components/production/FailedScenes.jsx"
      provides: "Failed scene recovery actions"
      min_lines: 60
  key_links:
    - from: "dashboard/src/pages/ProductionMonitor.jsx"
      to: "dashboard/src/hooks/useProductionProgress.js"
      via: "hook import"
      pattern: "useProductionProgress"
    - from: "dashboard/src/pages/ProductionMonitor.jsx"
      to: "dashboard/src/hooks/useProductionMutations.js"
      via: "hook import"
      pattern: "useProductionMutations"
    - from: "dashboard/src/components/production/DotGrid.jsx"
      to: "scenes data"
      via: "props from ProductionMonitor"
      pattern: "scenes.*map"
---

<objective>
Build the complete Production Monitor page with all sub-components: hero card, dot grid, queue, failed scenes, activity log, supervisor alert, and cost estimate dialog.

Purpose: This is the primary real-time production tracking interface (PROD-13). Users watch their video being produced scene by scene.
Output: 8 component files forming the complete Production Monitor page
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-production-pipeline/04-CONTEXT.md
@.planning/phases/04-production-pipeline/04-RESEARCH.md
@.planning/phases/04-production-pipeline/04-01-SUMMARY.md

@dashboard/src/hooks/useProductionProgress.js
@dashboard/src/hooks/useProductionMutations.js
@dashboard/src/hooks/useProductionLog.js
@dashboard/src/lib/api.js
@dashboard/src/components/ui/ConfirmDialog.jsx
@dashboard/src/components/ui/SkeletonLoader.jsx

<interfaces>
<!-- From Plan 04-01 hooks -->
From dashboard/src/hooks/useProductionProgress.js:
```javascript
export function useProductionProgress(topicId) {
  // Returns: { scenes, stageProgress, failedScenes, isLoading, error }
  // stageProgress: { audio: { done, total }, images: { done, total }, i2v: { done, total }, t2v: { done, total }, clips: { done, total } }
  // failedScenes: scene objects with failed audio/image/video status
}
```

From dashboard/src/hooks/useProductionMutations.js:
```javascript
export function useProductionMutations(projectId) {
  // Returns: { triggerProduction, stopProduction, resumeProduction, restartProduction,
  //            retryScene, skipScene, editAndRetryScene, retryAllFailed, skipAllFailed }
  // Each is a useMutation result with { mutate, isPending }
}
```

From dashboard/src/hooks/useProductionLog.js:
```javascript
export function useProductionLog(topicId) {
  // Returns: { logs, isLoading }
  // logs: [{ id, stage, action, details, created_at }]
}
```

From dashboard/src/components/ui/ConfirmDialog.jsx:
```jsx
// Centered modal with title, children, confirm/cancel buttons, variant prop (success/danger/primary)
<ConfirmDialog open={bool} onClose={fn} onConfirm={fn} title="..." variant="danger">
  {children}
</ConfirmDialog>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build production sub-components (HeroCard, DotGrid, QueueList, FailedScenes, ActivityLog, CostEstimateDialog, SupervisorAlert)</name>
  <files>
    dashboard/src/components/production/HeroCard.jsx
    dashboard/src/components/production/DotGrid.jsx
    dashboard/src/components/production/QueueList.jsx
    dashboard/src/components/production/FailedScenes.jsx
    dashboard/src/components/production/ActivityLog.jsx
    dashboard/src/components/production/CostEstimateDialog.jsx
    dashboard/src/components/production/SupervisorAlert.jsx
  </files>
  <behavior>
    - HeroCard: renders topic title, stage chips row, elapsed time, ETA, cost counter
    - DotGrid: groups scenes by chapter, colors dots by status, hover tooltip
    - QueueList: lists queued topics, remove button per topic
    - FailedScenes: per-scene retry/skip/edit-retry, batch actions
    - ActivityLog: collapsible production_log entries
    - CostEstimateDialog: ConfirmDialog wrapper showing per-stage cost breakdown
    - SupervisorAlert: amber banner when supervisor_alerted=true
  </behavior>
  <action>
Replace all 7 component stubs with full implementations. Follow glassmorphism design system (glass-card, badge-*, text-text-muted, animate-in patterns).

**HeroCard.jsx** (props: topic, stageProgress, elapsed, eta, cost):
- glass-card with gradient icon header (Activity icon)
- Topic title + topic_number badge
- Stage chips row: 5 chips (Audio, Images, I2V, T2V, Assembly). Use Mic, Image, Film, Film, Clapperboard icons from lucide-react.
  - Completed: badge-emerald + CheckCircle2 icon
  - Active: badge-primary + animate-pulse CSS class for subtle glow
  - Pending: opacity-40
- Elapsed time: formatted HH:MM:SS from elapsed prop (ms)
- ETA: "~X min remaining" from eta prop (ms), or "--" if not yet computed
- Cost: "$X.XX" from cost prop, with per-stage breakdown tooltip (script, tts, images, i2v, t2v)
- Stop Production button: red badge-red with StopCircle icon. Accepts onStop callback.

**DotGrid.jsx** (props: scenes):
- Group scenes by `chapter` field using reduce
- Render chapter name label above each group (text-[10px] uppercase tracking-wider text-text-muted)
- Each dot: w-2.5 h-2.5 rounded-sm with transition-colors duration-300
- Color logic per user decision:
  - gray (bg-slate-200 dark:bg-white/[0.06]) = all pending
  - blue (bg-blue-500) = audio_status='uploaded'
  - cyan (bg-cyan-500) = image_status='uploaded' (static_image scenes)
  - purple (bg-purple-500) = video_status='uploaded' (i2v/t2v scenes)
  - green (bg-emerald-500) = clip_status='complete' or 'uploaded'
  - red (bg-red-500) = any status is 'failed'
  - slate-400 (bg-slate-400) = skipped=true
- Hover title: "Scene {scene_number} ({chapter}): audio={audio_status}, image={image_status}, video={video_status}"
- Legend row at bottom: colored dots with labels (same as existing skeleton)

**QueueList.jsx** (props: queuedTopics, onRemove):
- Compact list styled like a playlist. Each row: topic_number badge, title (truncated 40 chars), "Remove" button
- Remove button calls onRemove(topicId) with existing ConfirmDialog
- Empty state: "Queue empty" text

**FailedScenes.jsx** (props: failedScenes, onRetry, onSkip, onEditRetry, onRetryAll, onSkipAll):
- Only renders when failedScenes.length > 0
- Header: "Failed Scenes ({count})" with AlertTriangle icon
- Each scene: scene_number, chapter, user-friendly error (extract from details or error_log)
- Expandable raw error: API name, error code, image_prompt text, attempt count (details in a collapsible div)
- Per-scene buttons: "Retry" (RefreshCw icon), "Edit & Retry" (Edit3 icon, opens textarea for image_prompt editing), "Skip" (SkipForward icon)
- Batch actions row at top: "Retry All Failed" button, "Skip All Failed" button
- Assembly gate message: "N failed scenes must be resolved before assembly" when any failures exist and all non-failed scenes are complete

**ActivityLog.jsx** (props: logs):
- Collapsible section, collapsed by default. Toggle button: "Activity Log ({count})"
- Each entry: timestamp (relative, e.g. "2m ago"), stage badge, action text, expandable details
- Max 10 entries shown, "Show more" link for overflow

**CostEstimateDialog.jsx** (props: open, onClose, onConfirm, topics, projectConfig):
- Uses ConfirmDialog with variant="primary"
- Lists topics to be queued with topic_number and title
- Cost breakdown: TTS (scene_count * per-unit), Images (image_count * per-unit), I2V (i2v_count * per-unit), T2V (t2v_count * per-unit)
- Total estimated cost prominently displayed
- "Start Production" confirm button

**SupervisorAlert.jsx** (props: visible, onDismiss):
- Amber warning banner: bg-amber-50 dark:bg-amber-900/20 border-amber-300 dark:border-amber-700
- AlertTriangle icon + "Supervisor detected a stuck production pipeline. Check failed scenes below."
- Dismiss X button
- Only renders when visible=true

Run DotGrid, HeroCard, FailedScenes, and SupervisorAlert tests to verify GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/DotGrid.test.jsx src/__tests__/HeroCard.test.jsx src/__tests__/FailedScenes.test.jsx src/__tests__/SupervisorAlert.test.jsx --reporter=verbose</automated>
  </verify>
  <done>All DotGrid, HeroCard, FailedScenes, SupervisorAlert tests pass. 7 component files implement full UI per CONTEXT.md decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite ProductionMonitor page with all sub-components wired</name>
  <files>dashboard/src/pages/ProductionMonitor.jsx</files>
  <behavior>
    - Empty state renders when no topic has status 'producing'
    - Active production renders HeroCard, DotGrid, FailedScenes, QueueList, ActivityLog
    - Supervisor alert renders when supervisor_alerted is true on active topic
    - Stop button triggers stopProduction mutation with ConfirmDialog
    - CostEstimateDialog opens on Start Production click
    - Auto-advance: success animation (3-5s all dots green) then switches to next queued topic
  </behavior>
  <action>
Full rewrite of `dashboard/src/pages/ProductionMonitor.jsx` (currently skeleton with hardcoded stages + 60 dots + ComingSoon). Remove all placeholder content.

Use useParams() to get projectId from route. Use useTopics(projectId) to get all topics. Derive:
- activeTopic = topics.find(t => t.status === 'producing')
- queuedTopics = topics.filter(t => t.status === 'queued').sort(by topic_number)
- recentlyCompleted = topics.filter(t => t.status === 'assembled').slice(-3).reverse()
- scriptApprovedCount = topics.filter(t => t.script_review_status === 'approved' && t.status === 'script_approved').length

Use useProductionProgress(activeTopic?.id) for scene-level data.
Use useProductionMutations(projectId) for all actions.
Use useProductionLog(activeTopic?.id) for activity log.

**Layout (per CONTEXT.md locked decisions):**
1. Page header: "Production Monitor" title + subtitle
2. **If no activeTopic:**
   - Empty state card: "No Active Production" + "{scriptApprovedCount} topics ready for production"
   - "Start Production" CTA button opens CostEstimateDialog listing script-approved topics
3. **If activeTopic:**
   - SupervisorAlert banner (conditionally)
   - HeroCard with stage chips, elapsed (computed from activeTopic.last_status_change to now), ETA (rolling average from stageProgress), cost (activeTopic.total_cost)
   - Stage summary chips row (derived from stageProgress)
   - DotGrid with scenes from useProductionProgress
   - FailedScenes (if failedScenes.length > 0)
   - QueueList with queuedTopics + remove handler
   - Recently Completed section: last 3 assembled topics showing topic #, title, cost, scene count
   - ActivityLog (collapsed by default)
4. Stop Production: ConfirmDialog with variant="danger" showing cost already spent, calls stopProduction
5. Resume/Restart: If activeTopic.status === 'stopped', show "Resume Production" and "Restart from Scratch" buttons. Restart requires typing 'RESTART' in a text input before confirm button enables.

**Success animation:** When activeTopic transitions to 'assembled' status (detected via Realtime), show a brief (3-5s) overlay: all dots turn green, checkmark icon, cost summary. Then auto-advance to next queued topic or show empty state.

**Topic Review integration:** Accept query param `?trigger=topicId` from Topic Review page redirect. If present, auto-open CostEstimateDialog for that topic.

Run ProductionMonitor tests to verify GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/ProductionMonitor.test.jsx --reporter=verbose</automated>
  </verify>
  <done>ProductionMonitor tests pass. Page renders empty state, active production view, and transitions correctly. All sub-components wired with real hooks.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run src/__tests__/ProductionMonitor.test.jsx src/__tests__/DotGrid.test.jsx src/__tests__/HeroCard.test.jsx src/__tests__/FailedScenes.test.jsx src/__tests__/SupervisorAlert.test.jsx --reporter=verbose` -- all pass
- `cd dashboard && npx vitest run --reporter=verbose` -- full suite passes
- `cd dashboard && npm run build` -- builds without errors
</verification>

<success_criteria>
- Production Monitor page fully replaced (no ComingSoon, no placeholder data)
- Empty state shows approved topic count and Start Production CTA
- Active production shows hero card, dot grid, queue, failed scenes, activity log
- Dot grid groups by chapter, colors by stage status
- Failed scenes section has retry/skip/edit-retry per scene and batch
- Supervisor alert banner displays when alerted
- Stop/Resume/Restart flows work with appropriate confirmation dialogs
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-pipeline/04-02-SUMMARY.md`
</output>
