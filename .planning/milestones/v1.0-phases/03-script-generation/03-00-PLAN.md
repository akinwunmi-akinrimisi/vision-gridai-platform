---
phase: 03-script-generation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/src/__tests__/ScriptReview.test.jsx
  - dashboard/src/__tests__/ScorePanel.test.jsx
  - dashboard/src/__tests__/ScriptActions.test.jsx
  - dashboard/src/__tests__/ScriptGenerate.test.jsx
  - dashboard/src/__tests__/useScenes.test.js
autonomous: true
requirements:
  - SCPT-10
  - SCPT-11
  - SCPT-12
  - SCPT-01
  - SCPT-05
  - SCPT-08
  - SCPT-09

must_haves:
  truths:
    - "All 5 test files exist and run (expected to fail — RED phase)"
    - "Tests describe the behaviors that will be implemented in Wave 1-2"
  artifacts:
    - path: "dashboard/src/__tests__/ScriptReview.test.jsx"
      provides: "Tests for SCPT-10, SCPT-05, SCPT-08 (page render, scores, visual badges)"
    - path: "dashboard/src/__tests__/ScorePanel.test.jsx"
      provides: "Tests for score panel dimensions, color thresholds, metadata display"
    - path: "dashboard/src/__tests__/ScriptActions.test.jsx"
      provides: "Tests for SCPT-11, SCPT-12 (approve/reject/refine mutations)"
    - path: "dashboard/src/__tests__/ScriptGenerate.test.jsx"
      provides: "Tests for SCPT-01 (generate button calls webhook)"
    - path: "dashboard/src/__tests__/useScenes.test.js"
      provides: "Tests for SCPT-09 (scenes hook fetches from Supabase)"
  key_links: []
---

<objective>
Wave 0 test scaffolds for Phase 3 Script Generation dashboard components.

Purpose: Create failing test stubs that define the expected behaviors for the Script Review page, score panel, scene display, approval actions, and data hooks. These tests become the verification criteria for Wave 1-2 implementation.

Output: 5 test files with descriptive test cases covering SCPT-01, SCPT-05, SCPT-08, SCPT-09, SCPT-10, SCPT-11, SCPT-12.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-script-generation/03-CONTEXT.md
@.planning/phases/03-script-generation/03-RESEARCH.md
@.planning/phases/03-script-generation/03-VALIDATION.md

@dashboard/src/__tests__/TopicReview.test.jsx (pattern reference for how Phase 2 tests are structured)
@dashboard/src/__tests__/TopicActions.test.jsx (pattern reference for mutation hook tests)
@dashboard/src/__tests__/setup.js (test setup file)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for Script Review page, ScorePanel, and scene components</name>
  <files>
    dashboard/src/__tests__/ScriptReview.test.jsx,
    dashboard/src/__tests__/ScorePanel.test.jsx,
    dashboard/src/__tests__/useScenes.test.js
  </files>
  <action>
Create 3 test files following the exact patterns from existing Phase 2 tests (TopicReview.test.jsx, TopicActions.test.jsx). Use vi.mock for supabase, react-router, and api modules.

**ScriptReview.test.jsx** — Tests for the full page:
- Renders header bar with topic title, playlist badge, status badge
- Renders score panel with 7 dimension bars
- Renders chapter accordions with scene counts
- First chapter is expanded by default, rest collapsed
- Clicking a chapter header toggles expand/collapse
- Renders scene rows with line numbers (monospace, 3-digit padded)
- Renders visual type badges (blue=static_image, purple=i2v, amber=t2v)
- Shows "Generate Script" CTA when topic has no script
- Shows pass tracker during generation (status='scripting')
- Shows force-pass amber warning when script_force_passed=true
- Prev/Next arrows navigate between approved topics
- Image prompts are hidden by default

**ScorePanel.test.jsx** — Tests for the score panel component:
- Renders overall quality score prominently
- Renders 7 dimension score bars with correct colors (>=8 green, >=7 amber, <7 red)
- Renders metadata: word count, scene count, visual split, attempts
- Renders action buttons: Approve, Reject, Refine
- Per-pass breakdown is collapsible (collapsed by default)
- Shows collapsible customer avatar section
- Shows collapsible YouTube metadata section

**useScenes.test.js** — Tests for the scenes data hook:
- Fetches scenes from Supabase filtered by topic_id, ordered by scene_number
- Returns empty array when no scenes exist
- Subscribes to Realtime on scenes table with topic_id filter
- Disabled when topicId is null/undefined

All tests should import from the correct paths (components not yet created — tests will fail). Use mock data matching the script_json and script_pass_scores JSONB structures from RESEARCH.md.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/ScriptReview.test.jsx src/__tests__/ScorePanel.test.jsx src/__tests__/useScenes.test.js --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>3 test files exist with descriptive test stubs. Tests run but are expected to fail (components don't exist yet).</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds for script actions and generation</name>
  <files>
    dashboard/src/__tests__/ScriptActions.test.jsx,
    dashboard/src/__tests__/ScriptGenerate.test.jsx
  </files>
  <action>
Create 2 test files for mutation hooks and generation flow.

**ScriptActions.test.jsx** — Tests for approve/reject/refine mutations:
- useApproveScript calls webhookCall('script/approve', { topic_id }) and invalidates ['script', topicId] query
- useRejectScript calls webhookCall('script/reject', { topic_id, feedback }) and invalidates ['script', topicId] query
- useRefineScript calls webhookCall('script/refine', { topic_id, instructions }) and invalidates ['script', topicId] query
- useRegenPrompts calls webhookCall('script/regen-prompts', { topic_id, scene_ids }) and invalidates ['scenes', topicId] query
- Optimistic update on approve sets script_review_status to 'approved'
- Optimistic update on reject sets script_review_status to 'rejected'
- Error rollback restores previous data

**ScriptGenerate.test.jsx** — Tests for generate script flow:
- useGenerateScript calls webhookCall('script/generate', { topic_id })
- Optimistic update sets topic status to 'scripting'
- Generate button is disabled when topic already has a script (script_json is not null)
- Generate button is disabled when topic is not approved (review_status != 'approved')

Follow the exact mutation testing pattern from TopicActions.test.jsx (mock webhookCall, wrap in QueryClientProvider, use renderHook).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/ScriptActions.test.jsx src/__tests__/ScriptGenerate.test.jsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>2 test files exist with descriptive test stubs covering script mutation hooks. Tests run but expected to fail.</done>
</task>

</tasks>

<verification>
All 5 test files exist and can be loaded by Vitest. They will fail because the components/hooks they test don't exist yet — this is expected (RED phase of TDD). The test descriptions accurately reflect the behaviors that Wave 1-2 will implement.

```bash
cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -30
```
</verification>

<success_criteria>
- 5 test files created in dashboard/src/__tests__/
- Tests reference the correct component/hook import paths
- Test descriptions match SCPT requirements
- Vitest can load and attempt to run all tests
- Existing Phase 2 tests are NOT broken
</success_criteria>

<output>
After completion, create `.planning/phases/03-script-generation/03-00-SUMMARY.md`
</output>
