---
phase: 05-publish-analytics
plan: 02
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - dashboard/src/pages/Analytics.jsx
  - dashboard/src/components/analytics/TopPerformerCard.jsx
  - dashboard/src/components/analytics/ViewsChart.jsx
  - dashboard/src/components/analytics/RevenueChart.jsx
  - dashboard/src/components/analytics/PerformanceChart.jsx
  - dashboard/src/components/analytics/CostDonut.jsx
  - dashboard/src/components/analytics/CostRevenueChart.jsx
  - dashboard/src/components/analytics/PerformanceTable.jsx
  - dashboard/src/components/analytics/TimeRangeFilter.jsx
autonomous: true
requirements:
  - ANLY-03
  - ANLY-04

must_haves:
  truths:
    - "User can see total views, watch hours, CTR, and revenue on the Analytics page"
    - "User can see which video performs best (top performer card)"
    - "User can see views over time as an area chart"
    - "User can see cost distribution by production stage as a donut chart"
    - "User can see cost vs revenue per video as a grouped bar chart"
    - "User can sort videos by any metric column in the performance table"
    - "User can filter analytics by time range (7d, 30d, 90d, all)"
  artifacts:
    - path: "dashboard/src/pages/Analytics.jsx"
      provides: "Full analytics page with charts and table"
      min_lines: 100
    - path: "dashboard/src/components/analytics/CostDonut.jsx"
      provides: "Cost distribution donut chart"
      min_lines: 30
    - path: "dashboard/src/components/analytics/PerformanceTable.jsx"
      provides: "Sortable per-video performance table"
      min_lines: 60
  key_links:
    - from: "dashboard/src/pages/Analytics.jsx"
      to: "dashboard/src/hooks/useAnalytics.js"
      via: "hook call"
      pattern: "useAnalytics\\(id"
    - from: "dashboard/src/components/analytics/PerformanceTable.jsx"
      to: "dashboard/src/pages/VideoReview.jsx"
      via: "navigate on row click"
      pattern: "navigate.*review"
---

<objective>
Build the Analytics page with Recharts charts, metric cards, and sortable performance table.

Purpose: Users need to see YouTube performance data (views, revenue, CTR) and cost tracking across all published videos. This page closes the feedback loop — see what works, optimize future content.

Output: Complete Analytics page with 5 chart types, metric cards, top performer highlight, time range filter, and sortable per-video table.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-publish-analytics/05-CONTEXT.md
@.planning/phases/05-publish-analytics/05-RESEARCH.md
@.planning/phases/05-publish-analytics/05-00-SUMMARY.md

@dashboard/src/pages/Analytics.jsx
@dashboard/src/hooks/useAnalytics.js
@dashboard/src/hooks/useTheme.js
@dashboard/src/lib/analyticsApi.js
@design-system/MASTER.md

<interfaces>
From dashboard/src/hooks/useAnalytics.js:
```javascript
export function useAnalytics(projectId, timeRange) { ... }
// timeRange: '7d' | '30d' | '90d' | 'all'
// Returns: { data: Topic[], isLoading, error }
// Also computes: totalViews, totalWatchHours, avgCtr, totalRevenue, totalLikes,
//   totalComments, totalSubscribers, avgDuration, topPerformer
```

From dashboard/src/hooks/useTheme.js:
```javascript
export function useTheme() { ... }
// Returns: { isDark, toggleTheme }
```

From dashboard/src/lib/analyticsApi.js:
```javascript
export const refreshAnalytics = (projectId) => webhookCall('analytics/refresh', { project_id: projectId });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics chart components</name>
  <files>
    dashboard/src/components/analytics/TopPerformerCard.jsx,
    dashboard/src/components/analytics/ViewsChart.jsx,
    dashboard/src/components/analytics/RevenueChart.jsx,
    dashboard/src/components/analytics/PerformanceChart.jsx,
    dashboard/src/components/analytics/CostDonut.jsx,
    dashboard/src/components/analytics/CostRevenueChart.jsx,
    dashboard/src/components/analytics/TimeRangeFilter.jsx
  </files>
  <action>
All charts use Recharts (already installed at ^2.10.0). Use useTheme() to get isDark for theme-aware colors.

Chart color constants (define in each component or a shared constants object):
```
primary: '#2563EB' (blue - main metrics)
green: '#10B981' (revenue/profit)
amber: '#F59E0B' (cost/spend)
purple: '#8B5CF6' (secondary metrics)
red: '#EF4444' (loss/negative)
grid: isDark ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.06)'
text: isDark ? '#94A3B8' : '#64748B'
```

Implement TopPerformerCard.jsx:
- Props: { topic } (the topic with highest views)
- If no topic (no published videos yet), render nothing
- Card with gradient border effect: `relative glass-card p-6 overflow-hidden` with a `before:` gradient pseudo-element or a thin gradient top border (bg-gradient-to-r from-primary to-emerald-500 h-1 rounded-t-xl)
- Show: thumbnail (small, rounded), SEO title, key metrics inline (Views, Watch Hours, Revenue, CTR as small stat chips)
- Trophy icon (lucide Trophy) in header
- "Top Performer" heading

Implement ViewsChart.jsx:
- Props: { data } — array of { date: string, views: number } (transform topics to daily aggregates in parent)
- Recharts AreaChart in ResponsiveContainer (width 100%, parent div h-64)
- CartesianGrid with strokeDasharray="3 3", stroke={grid color}
- XAxis dataKey="date", tick fontSize 12, fill text color
- YAxis tick fontSize 12, fill text color
- Tooltip with glass-card-style custom tooltip (backgroundColor matches theme)
- Area type="monotone" dataKey="views" stroke={primary} fill={primary} fillOpacity={0.15}
- glass-card wrapper with "Views Over Time" heading

Implement RevenueChart.jsx:
- Props: { data } — array of { date: string, revenue: number }
- Recharts LineChart in ResponsiveContainer (parent h-64)
- Same axis/grid pattern as ViewsChart
- Line type="monotone" dataKey="revenue" stroke={green} strokeWidth={2} dot={false}
- Tooltip formatter: (val) => `$${val.toFixed(2)}`
- glass-card wrapper with "Revenue Over Time" heading

Implement PerformanceChart.jsx:
- Props: { data } — array of { title: string (truncated to 20 chars), ctr: number, avgDuration: number }
- Recharts BarChart in ResponsiveContainer (parent h-64)
- Two bars: CTR (primary blue, dataKey="ctr") and Avg Duration seconds (purple, dataKey="avgDuration")
- XAxis dataKey="title", tick angle={-45}, textAnchor="end", height={80}, fontSize 11
- Legend at top
- glass-card wrapper with "CTR & Avg Duration per Video" heading

Implement CostDonut.jsx:
- Props: { costBreakdown } — aggregated { script, tts, images, i2v, t2v } across all topics
- Recharts PieChart with Pie component (innerRadius={60}, outerRadius={90} for donut effect)
- Cell colors: Script=#3B82F6, TTS=#8B5CF6, Images=#10B981, I2V=#F59E0B, T2V=#EF4444
- Tooltip formatter: (val) => `$${val.toFixed(2)}`
- Legend at bottom
- Center text showing total cost (position absolute, centered)
- glass-card wrapper with "Cost Distribution" heading
- If no cost data, show empty state placeholder

Implement CostRevenueChart.jsx:
- Props: { data } — array of { title: string, cost: number, revenue: number }
- Recharts BarChart with grouped bars
- Two bars: Cost (amber, dataKey="cost") and Revenue (green, dataKey="revenue")
- XAxis dataKey="title", same angled labels as PerformanceChart
- Tooltip showing both values with $ formatting
- glass-card wrapper with "Cost vs Revenue per Video" heading

Implement TimeRangeFilter.jsx:
- Props: { value, onChange }
- Use the existing FilterDropdown component pattern if available, otherwise simple select/dropdown
- Options: { label: 'Last 7 days', value: '7d' }, { label: 'Last 30 days', value: '30d' }, { label: 'Last 90 days', value: '90d' }, { label: 'All time', value: 'all' }
- Styled as a glass-card inline dropdown with Calendar icon from lucide
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run src/__tests__/Analytics.test.jsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 chart/filter components implemented with Recharts. Theme-aware colors work in light/dark mode. Charts use ResponsiveContainer with fixed-height parents. CostDonut shows donut with center total. TopPerformerCard shows gradient border highlight.</done>
</task>

<task type="auto">
  <name>Task 2: Analytics page rewrite + PerformanceTable</name>
  <files>
    dashboard/src/pages/Analytics.jsx,
    dashboard/src/components/analytics/PerformanceTable.jsx
  </files>
  <action>
Implement PerformanceTable.jsx:
- Props: { topics, projectId } — array of published topics with yt_* and cost data
- Sortable table with columns: #, Title, Views, Watch Hours, CTR, Avg Duration, Revenue, Cost, P/L, CPM, Published Date
- Local state for sortColumn and sortDirection (asc/desc), default sort: published_at desc
- Click column header to toggle sort. Active sort column shows ChevronUp/ChevronDown icon.
- Table uses overflow-x-auto for mobile responsiveness (same pattern as PipelineTable)
- P/L column: revenue - cost, shown in green if positive with "+" prefix, red if negative. Use font-semibold for emphasis.
- Title column truncated with `truncate max-w-[200px]` class
- Clicking a row navigates to `/project/${projectId}/topics/${topicId}/review` (Video Review in read-only mode for published videos)
- Row hover effect: `hover:bg-white/5 cursor-pointer transition-colors`
- Format values: views as locale number (1,234), watch hours as X.Xh, CTR as X.X%, duration as M:SS, revenue/cost/P-L as $X.XX, CPM as $X.XX, date as MMM DD, YYYY
- If no published topics: show empty state with BarChart3 icon and "No published videos yet" message
- glass-card wrapper

Rewrite Analytics.jsx (replace the current scaffold with full implementation):
- Import useParams from 'react-router', extract id
- Import useAnalytics(id, timeRange), useProjectMetrics(id)
- Local state: timeRange (default '30d')
- Page header: "Analytics" title + "Last updated: {date}" subtitle + "Refresh Now" button (calls refreshAnalytics(id) from analyticsApi, show loading spinner while pending, use useMutation)
- TimeRangeFilter in header area, right-aligned

**Layout:**
1. Primary metric cards (same 4 as current scaffold but wired to real data):
   - Total Views: analytics.totalViews formatted with locale
   - Watch Hours: analytics.totalWatchHours formatted as X.Xh
   - Avg CTR: analytics.avgCtr formatted as X.X%
   - Revenue: analytics.totalRevenue formatted as $X.XX
   - Keep existing gradient icon backgrounds

2. Secondary metric cards (same 4, wired):
   - Likes: analytics.totalLikes
   - Comments: analytics.totalComments
   - Subscribers Gained: analytics.totalSubscribers
   - Avg View Duration: analytics.avgDuration

3. TopPerformerCard with analytics.topPerformer topic

4. Charts in 2-column grid (lg:grid-cols-2):
   - ViewsChart: Transform topics to daily data points. Group by published_at date, sum views per date. Sort chronologically.
   - RevenueChart: Same transformation with yt_estimated_revenue
   - PerformanceChart: Map topics to { title: truncated seo_title, ctr: yt_ctr, avgDuration: parsed yt_avg_view_duration in seconds }
   - CostDonut: Aggregate cost_breakdown across all topics (sum each stage)
   - CostRevenueChart: Map topics to { title: truncated seo_title, cost: total_cost, revenue: yt_estimated_revenue }

5. PerformanceTable with full topic list

- Loading state: show SkeletonLoader cards
- Empty state (no published topics): show gentle message with BarChart3 icon
- Remove ComingSoon component import and usage
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run src/__tests__/Analytics.test.jsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Analytics page renders with real data from useAnalytics hook. 4 primary + 4 secondary metric cards show real values. Top performer card highlights best video. 5 chart types render correctly with theme-aware colors. PerformanceTable is sortable with all columns. Time range filter changes displayed data. Refresh Now button triggers analytics webhook. Row click navigates to Video Review.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run --reporter=verbose` — full suite passes
- Analytics page renders charts (no 0-height containers)
- All chart components wrapped in fixed-height parent divs
- PerformanceTable sorts by all columns correctly
- Time range filter updates displayed data
- No ComingSoon component on Analytics page
</verification>

<success_criteria>
- Analytics page fully functional with 5 chart types using Recharts
- 4 primary + 4 secondary metric cards wired to real data
- Top performer card with gradient border highlights best video
- Sortable performance table with P/L column (green/red)
- Time range filter (7d, 30d, 90d, all) works
- Refresh Now button triggers analytics webhook
- Dark mode compatible (theme-aware chart colors)
- Cost donut shows per-stage breakdown
- Cost vs Revenue grouped bar chart shows profitability
</success_criteria>

<output>
After completion, create `.planning/phases/05-publish-analytics/05-02-SUMMARY.md`
</output>
