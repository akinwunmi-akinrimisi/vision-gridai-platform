---
phase: 09-ai-agent-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/WF_PROJECT_CREATE.json
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-07
  - AGNT-08

must_haves:
  truths:
    - "Niche research Claude API call has timeout 600000ms (600s)"
    - "Re-triggering research on a project with existing niche_profile skips Claude web search and only re-generates prompts"
    - "prompt_configs rows created with version = MAX(version)+1 and old rows set is_active = false on re-trigger"
    - "production_log INSERT nodes fire at workflow start (action=started) and end (action=completed or action=failed)"
    - "All failure branches (Claude research error, Claude prompts error, Supabase write error) connect to error handler that writes projects.status=research_failed and projects.error_log"
  artifacts:
    - path: "workflows/WF_PROJECT_CREATE.json"
      provides: "Hardened niche research + prompt generation workflow"
      contains: "timeout: 600000"
  key_links:
    - from: "Check Niche Profile node (HTTP GET niche_profiles)"
      to: "Idempotency IF node"
      via: "count check on response array length"
      pattern: "niche_profiles.*project_id"
    - from: "Claude research error output"
      to: "Error Handler node (PATCH projects status=research_failed)"
      via: "onError: continueRegularOutput + IF isError branch"
      pattern: "research_failed"
    - from: "Re-generate prompts path"
      to: "prompt_configs PATCH (deactivate old) then POST (new version)"
      via: "Code node computing MAX(version)+1"
      pattern: "is_active.*false"
---

<objective>
Harden WF_PROJECT_CREATE: increase Claude research timeout to 600s, add idempotency guard (skip web search if niche_profile exists, re-generate prompts only), add production_log entries for started/completed/failed, and connect error handlers on all failure branches.

Purpose: Requirements AGNT-01, AGNT-02, AGNT-03, AGNT-07, AGNT-08 are currently unmet. The workflow has an error handler node but it has NO incoming connections — error paths from the Claude research call and Claude prompts call are disconnected. There is no idempotency check, so re-triggering research overwrites the niche_profiles row. There are no production_log writes anywhere.

Output: Updated workflows/WF_PROJECT_CREATE.json deployed to n8n (ID: 8KW1hiRklamduMzO) and activated.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-agent-workflows/09-CONTEXT.md
@.planning/phases/09-ai-agent-workflows/09-RESEARCH.md

<!-- Key workflow IDs (from STATE.md) -->
<!-- WF_PROJECT_CREATE n8n ID: 8KW1hiRklamduMzO -->
<!-- n8n API: https://n8n.srv1297445.hstgr.cloud/api/v1/ -->
<!-- N8N_API_KEY: must be retrieved from n8n Settings > API or stored env var -->
<!-- Supabase URL: https://supabase.operscale.cloud -->

<interfaces>
<!-- n8n workflow update pattern (from Phase 8 confirmed approach) -->
<!-- 1. GET /api/v1/workflows/8KW1hiRklamduMzO to fetch current live JSON -->
<!-- 2. Modify JSON locally (add nodes, connections, update timeout) -->
<!-- 3. Write to temp file -->
<!-- 4. PUT /api/v1/workflows/8KW1hiRklamduMzO with full JSON from temp file -->
<!-- 5. POST /api/v1/workflows/8KW1hiRklamduMzO/activate -->

<!-- Supabase write pattern (used throughout WF_PROJECT_CREATE already) -->
<!-- PATCH https://supabase.operscale.cloud/rest/v1/projects?id=eq.{id} -->
<!-- Headers: apikey + Authorization Bearer + Content-Type: application/json + Prefer: return=minimal -->

<!-- production_log INSERT pattern -->
<!-- POST https://supabase.operscale.cloud/rest/v1/production_log -->
<!-- Body: { project_id, topic_id: null, stage: 'niche_research', action: 'started'|'completed'|'failed', details: {...} } -->

<!-- n8n error handler pattern (from RESEARCH.md) -->
<!-- onError: 'continueRegularOutput' on HTTP Request node -->
<!-- Downstream Code node checks response.error -->
<!-- Returns { isError: true, error_message, project_id } if error -->
<!-- IF node branches: isError=true → error path (PATCH status + production_log) | false → continue -->

<!-- Idempotency: research re-trigger -->
<!-- GET niche_profiles?project_id=eq.{id}&select=id -->
<!-- If response array.length > 0 → skip_research=true, use existing project data for prompts -->
<!-- If response array.length === 0 → fresh research (normal path) -->

<!-- Prompt versioning on re-trigger -->
<!-- Step 1: GET prompt_configs?project_id=eq.{id}&select=version&order=version.desc&limit=1 -->
<!-- Step 2: PATCH prompt_configs?project_id=eq.{id}&is_active=eq.true → set is_active=false -->
<!-- Step 3: POST prompt_configs with version = MAX(version)+1 and is_active=true (7 rows) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotency guard and production_log nodes to WF_PROJECT_CREATE</name>
  <files>workflows/WF_PROJECT_CREATE.json</files>
  <action>
SSH to VPS (root@srv1297445.hstgr.cloud). Obtain N8N_API_KEY from n8n Settings > API (generate if absent).

Fetch current live workflow JSON:
```
curl -s -H "Authorization: Bearer {N8N_API_KEY}" \
  https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/8KW1hiRklamduMzO \
  > /tmp/wf_project_create_live.json
```

Read the local workflows/WF_PROJECT_CREATE.json for node IDs and structure reference.

Modify the workflow JSON to add these nodes and connections. Work against the LIVE JSON (not local) since node positions may differ after import:

**Node additions (insert into `nodes` array):**

1. **"Check Niche Profile"** (HTTP Request, GET niche_profiles):
   - URL: `{{$env.SUPABASE_URL}}/rest/v1/niche_profiles?project_id=eq.{{$json.project_id}}&select=id`
   - Method: GET
   - Headers: apikey + Authorization Bearer SUPABASE_SERVICE_ROLE_KEY
   - Position: after "Read Project" node, before research branch

2. **"Idempotency Check"** (Code node):
   - Logic: read Check Niche Profile response; if array.length > 0, set `skip_research=true`; else `skip_research=false`; pass through project_id

3. **"IF Skip Research"** (IF node):
   - Condition: `{{ $json.skip_research }}` equals true
   - True branch → "Get Existing Prompts Version" (for re-prompt path)
   - False branch → existing Claude research call (normal path)

4. **"Get Existing Prompts Version"** (HTTP Request, GET prompt_configs version):
   - URL: `{{$env.SUPABASE_URL}}/rest/v1/prompt_configs?project_id=eq.{{$json.project_id}}&select=version&order=version.desc&limit=1`
   - On success → passes max_version to next node

5. **"Deactivate Old Prompts"** (HTTP Request, PATCH prompt_configs):
   - URL: `{{$env.SUPABASE_URL}}/rest/v1/prompt_configs?project_id=eq.{{$json.project_id}}&is_active=eq.true`
   - Method: PATCH
   - Body: `{ "is_active": false }`
   - Flows into existing "Generate Prompts" Claude call using cached project data

6. **"Log Started"** (HTTP Request, POST production_log):
   - Position: first node after webhook trigger (before any work)
   - Body: `{ "project_id": "{{$json.project_id}}", "topic_id": null, "stage": "niche_research", "action": "started", "details": {} }`

7. **"Log Completed"** (HTTP Request, POST production_log):
   - Position: final node on success path (after all prompts inserted)
   - Body: `{ "project_id": "{{$json.project_id}}", "topic_id": null, "stage": "niche_research", "action": "completed", "details": { "prompts_generated": 7 } }`

8. **"Log Failed"** (HTTP Request, POST production_log):
   - Body: `{ "project_id": "{{$json.project_id}}", "topic_id": null, "stage": "niche_research", "action": "failed", "details": { "error": "{{$json.error_message}}" } }`
   - Called from error branches (see Task 2)

**Timeout fix:** Find node `wf-pc-anthropic-research` in the nodes array. In its `parameters.options` (or add `options` if absent), set `"timeout": 600000`.

After modifying the JSON, write it to `/tmp/wf_project_create_updated.json` and validate it parses correctly with `python3 -c "import json; json.load(open('/tmp/wf_project_create_updated.json'))"`.

Deploy: `curl -s -X PUT -H "Authorization: Bearer {N8N_API_KEY}" -H "Content-Type: application/json" -d @/tmp/wf_project_create_updated.json https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/8KW1hiRklamduMzO`

Activate: `curl -s -X POST -H "Authorization: Bearer {N8N_API_KEY}" https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/8KW1hiRklamduMzO/activate`

Copy the deployed JSON back to the local repository file `workflows/WF_PROJECT_CREATE.json` so source of truth stays in sync.
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform && python3 -c "import json; wf=json.load(open('workflows/WF_PROJECT_CREATE.json')); nodes={n['name']:n for n in wf['nodes']}; assert 'Check Niche Profile' in nodes, 'Missing idempotency check node'; timeout=nodes.get('wf-pc-anthropic-research',{}).get('parameters',{}).get('options',{}).get('timeout'); assert timeout==600000, f'Timeout not 600000, got {timeout}'; print('PASS: idempotency node present, timeout=600000')"</automated>
  </verify>
  <done>
    - `Check Niche Profile` HTTP Request node exists in workflow
    - `IF Skip Research` routes fresh vs. re-trigger paths correctly
    - `wf-pc-anthropic-research` node has timeout=600000
    - `Log Started` node fires immediately after webhook entry
    - `Log Completed` node fires after successful prompt insertion
    - Workflow is active in n8n
    - Local workflows/WF_PROJECT_CREATE.json matches deployed version
  </done>
</task>

<task type="auto">
  <name>Task 2: Connect error handlers on all WF_PROJECT_CREATE failure branches</name>
  <files>workflows/WF_PROJECT_CREATE.json</files>
  <action>
Continuing the workflow modification from Task 1 (or apply as a second update round if simpler):

WF_PROJECT_CREATE already has an error handler node but it is NOT connected to the Claude research or Claude prompts call failure outputs. Fix this by adding explicit error-check Code nodes and IF branches after each Claude call, using the `onError: 'continueRegularOutput'` pattern.

**For node `wf-pc-anthropic-research` (niche research Claude call):**
1. Set `"onError": "continueRegularOutput"` in node parameters
2. Add downstream Code node "Check Research Error":
   ```javascript
   const response = $input.first().json;
   if (response.error || response.type === 'error') {
     return [{ json: {
       isError: true,
       error_message: response.error?.message || JSON.stringify(response.error || response),
       project_id: $('Validate').first().json.project_id
     }}];
   }
   return [{ json: { ...response, isError: false } }];
   ```
3. Add IF node "IF Research Error": `{{ $json.isError }}` true → error path | false → continue
4. Error path: "PATCH Research Failed" → PATCH projects `{ "status": "research_failed", "error_log": "{{$json.error_message}}" }` → "Log Failed" (POST production_log)

**For node `wf-pc-anthropic-prompts` (prompt generation Claude call):**
1. Same pattern: `onError: 'continueRegularOutput'` + Code node "Check Prompts Error" + IF node + error path to PATCH + Log Failed

**Ensure the existing error handler node** (if it has position data in the JSON) is replaced by these explicit branch patterns — inline error handling is more reliable than n8n's global error trigger for this workflow.

After making changes, deploy the updated JSON:
```
curl -s -X PUT -H "Authorization: Bearer {N8N_API_KEY}" -H "Content-Type: application/json" \
  -d @/tmp/wf_project_create_updated.json \
  https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/8KW1hiRklamduMzO
```

Activate and sync local file.
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform && python3 -c "
import json
wf = json.load(open('workflows/WF_PROJECT_CREATE.json'))
nodes = {n['name']: n for n in wf['nodes']}
# Check onError on research node
research_node = nodes.get('wf-pc-anthropic-research', {})
assert research_node.get('onError') == 'continueRegularOutput' or research_node.get('parameters', {}).get('onError') == 'continueRegularOutput', 'Research node missing onError'
# Check error check code nodes exist
assert 'Check Research Error' in nodes, 'Missing Check Research Error node'
# Check Log Failed node exists
assert 'Log Failed' in nodes, 'Missing Log Failed node'
print('PASS: error handler nodes present and connected')
"</automated>
  </verify>
  <done>
    - Both Claude call nodes have `onError: 'continueRegularOutput'`
    - `Check Research Error` Code node routes errors to status-failed PATCH
    - `Check Prompts Error` Code node routes errors to status-failed PATCH
    - Both error paths write `projects.status = 'research_failed'` and `projects.error_log`
    - Both error paths call `Log Failed` (POST production_log with action=failed)
    - Workflow active in n8n
    - Local workflows/WF_PROJECT_CREATE.json updated and committed
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Trigger WF_PROJECT_CREATE via webhook with a test project_id that already has a niche_profile — confirm response comes back without triggering Claude web search (check n8n execution log: research node should be skipped)
2. Check workflows/WF_PROJECT_CREATE.json has all required nodes: `Check Niche Profile`, `IF Skip Research`, `Log Started`, `Log Completed`, `Log Failed`, `Check Research Error`, timeout=600000 on research node
3. Run: `python3 -c "import json; wf=json.load(open('workflows/WF_PROJECT_CREATE.json')); print('nodes:', [n['name'] for n in wf['nodes']])"`
</verification>

<success_criteria>
- WF_PROJECT_CREATE deployed and active in n8n with all Phase 9 hardening
- Re-triggering research skips Claude web search when niche_profile exists (idempotency)
- Prompt re-generation increments version and deactivates old rows
- production_log entries appear in ProductionMonitor when workflow runs
- Any failure writes projects.status=research_failed (verifiable via bad API key test)
- Claude research timeout is 600000ms (not original 120000ms)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-agent-workflows/09-01-SUMMARY.md`
</output>
