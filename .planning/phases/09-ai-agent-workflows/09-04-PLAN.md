---
phase: 09-ai-agent-workflows
plan: 04
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - dashboard/src/components/topics/TopicCard.jsx
  - dashboard/src/hooks/useTopics.js
  - dashboard/src/hooks/useProjects.js
  - dashboard/src/components/projects/ProjectCard.jsx
  - dashboard/src/pages/TopicReview.jsx
  - dashboard/src/__tests__/TopicActions.test.jsx
  - dashboard/src/__tests__/TopicReview.test.jsx
  - dashboard/src/__tests__/ProjectsHome.test.jsx
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Pencil icon on TopicCard enters inline edit mode — all topic and avatar fields become controlled inputs within the expanded section"
    - "Save sends two webhook calls: action='edit' for topic fields, action='edit_avatar' for avatar fields"
    - "Cancel resets inputs to original values without saving"
    - "useEditTopic hook POSTs to 'topics/action' (not 'topics/edit') with { action: 'edit', topic_id, fields }"
    - "Retry button appears on ProjectCard when project.status = 'research_failed'"
    - "Retry calls /webhook/project/create with the existing project_id"
    - "TopicReview page no longer opens EditPanel SidePanel on edit — it passes onEdit down to TopicCard which handles edit mode inline"
  artifacts:
    - path: "dashboard/src/components/topics/TopicCard.jsx"
      provides: "Inline edit mode with controlled inputs for topic + avatar fields"
      contains: "isEditing"
    - path: "dashboard/src/hooks/useTopics.js"
      provides: "Fixed useEditTopic (topics/action path) + new useEditAvatar hook"
      contains: "edit_avatar"
    - path: "dashboard/src/hooks/useProjects.js"
      provides: "useRetryResearch hook"
      contains: "useRetryResearch"
    - path: "dashboard/src/components/projects/ProjectCard.jsx"
      provides: "Retry button on research_failed status"
      contains: "research_failed"
  key_links:
    - from: "TopicCard Pencil button click"
      to: "setIsEditing(true)"
      via: "isEditing state in TopicCard component"
      pattern: "isEditing"
    - from: "TopicCard Save button"
      to: "useEditTopic + useEditAvatar mutations"
      via: "onSave prop or internal hook calls"
      pattern: "edit_avatar"
    - from: "useEditTopic mutationFn"
      to: "webhookCall('topics/action', { action: 'edit', topic_id, fields })"
      via: "webhookCall in lib/api.js"
      pattern: "topics/action.*edit"
    - from: "ProjectCard Retry button"
      to: "useRetryResearch mutationFn"
      via: "onClick handler"
      pattern: "useRetryResearch"
---

<objective>
Implement DASH-01 inline editing: convert TopicCard's expanded section to support inline edit mode with controlled inputs for 3 topic fields and 10 avatar fields; fix the useEditTopic webhook path bug (topics/edit → topics/action with action='edit'); add useEditAvatar hook; add useRetryResearch hook and Retry button to ProjectCard for research_failed status; remove EditPanel SidePanel from TopicReview; update failing test stubs to pass.

Purpose: DASH-01 is the only dashboard requirement for Phase 9. The existing EditPanel SidePanel approach is replaced by inline edit mode per user decision in CONTEXT.md. The useEditTopic bug (wrong webhook path) means edit is currently broken at the API level. ProjectCard already shows research_failed badge but has no retry action.

Output: Updated dashboard files deployed to VPS. All failing test stubs replaced with passing tests.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-ai-agent-workflows/09-CONTEXT.md

<!-- Design system: read design-system/MASTER.md before any UI changes -->
<!-- TopicCard.jsx: already has AVATAR_FIELDS const (lines 24-35), onEdit callback (line 220), expanded section (lines 246-287) -->
<!-- useTopics.js: useEditTopic currently uses webhookCall('topics/edit', ...) — wrong path -->
<!-- useProjects.js: useCreateProject exists but no useRetryResearch hook -->
<!-- ProjectCard.jsx: STATUS_BADGE already has 'research_failed' → badge-red style (line 17). No Retry button exists. -->
<!-- TopicReview.jsx: imports EditPanel, has panelType/panelTopic state, has handleEdit that opens EditPanel SidePanel -->
<!-- EditPanel.jsx: SidePanel component — will be REMOVED from TopicReview.jsx render, not deleted -->

<interfaces>
<!-- TopicCard.jsx edit mode state additions -->
<!--
const [isEditing, setIsEditing] = useState(false);
const [editTopicFields, setEditTopicFields] = useState({});
const [editAvatarFields, setEditAvatarFields] = useState({});

// Initialize edit state when entering edit mode
const handleEnterEdit = (e) => {
  e.stopPropagation();
  setEditTopicFields({
    seo_title: topic.seo_title || '',
    narrative_hook: topic.narrative_hook || '',
    key_segments: topic.key_segments || '',
  });
  const av = topic.avatars?.[0] || {};
  const af = {};
  for (const { key } of AVATAR_FIELDS) { af[key] = av[key] || ''; }
  setEditAvatarFields(af);
  setIsEditing(true);
};
-->

<!-- TopicCard save handler -->
<!--
const handleSave = async () => {
  try {
    // Two parallel webhook calls
    await Promise.all([
      onSave({ topic_id: topic.id, fields: editTopicFields }),         // edit action
      onSaveAvatar({ topic_id: topic.id, fields: editAvatarFields }),  // edit_avatar action
    ]);
    setIsEditing(false);
  } catch (err) {
    // error stays visible — isSaving state shows loading
  }
};
-->

<!-- useEditTopic fix (corrected webhook path) -->
<!--
export function useEditTopic(projectId) {
  ...
  mutationFn: ({ topic_id, fields }) =>
    webhookCall('topics/action', { action: 'edit', topic_id, fields }),
  ...
}
-->

<!-- useEditAvatar (new hook, same pattern as useEditTopic) -->
<!--
export function useEditAvatar(projectId) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ topic_id, fields }) =>
      webhookCall('topics/action', { action: 'edit_avatar', topic_id, fields }),
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['topics', projectId] });
    },
  });
}
-->

<!-- useRetryResearch (new hook in useProjects.js) -->
<!--
export function useRetryResearch() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ project_id }) =>
      webhookCall('project/create', { project_id }),
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['projects'] });
    },
  });
}
-->

<!-- TopicReview.jsx changes -->
<!-- Remove: import EditPanel, panelType/panelTopic state for edit, handleEdit function, EditPanel render -->
<!-- Remove: onEdit={handleEdit} prop from TopicCard (or keep it as a no-op/inline trigger) -->
<!-- TopicCard now handles edit mode internally — TopicReview passes onSave + onSaveAvatar mutations down -->
<!-- Keep: RefinePanel (refine still uses SidePanel pattern — only edit mode moves inline) -->

<!-- ProjectCard.jsx Retry button -->
<!-- Current structure: NavLink wrapping the whole card — Retry button must stopPropagation -->
<!-- When status === 'research_failed': render a Retry button (RefreshCw icon) -->
<!-- Button calls onRetry prop from ProjectsHome → useRetryResearch mutation -->
<!-- ProjectCard needs `onRetry` prop added -->
<!-- ProjectsHome needs to pass onRetry={retryMutation.mutate} to each ProjectCard -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Update test stubs to reflect inline edit requirements</name>
  <files>
    dashboard/src/__tests__/TopicActions.test.jsx
    dashboard/src/__tests__/TopicReview.test.jsx
    dashboard/src/__tests__/ProjectsHome.test.jsx
  </files>
  <behavior>
    - TopicActions: Pencil button on non-approved topic toggles isEditing — expanded section shows text inputs instead of read-only text
    - TopicActions: Save button calls onSave and onSaveAvatar with correct fields
    - TopicActions: Cancel button exits edit mode without calling onSave
    - TopicActions: useEditTopic mutationFn posts to 'topics/action' with action='edit' (not 'topics/edit')
    - TopicActions: useEditAvatar mutationFn posts to 'topics/action' with action='edit_avatar'
    - TopicReview: edit action does NOT open EditPanel SidePanel (panelType should not be set to 'edit')
    - TopicReview: topics with topics_exist:true flag shows confirm dialog before generating more
    - ProjectsHome: research_failed project card shows Retry button
    - ProjectsHome: Retry button calls useRetryResearch with project_id
  </behavior>
  <action>
Replace all `expect(true).toBe(false)` RED stubs in TopicActions.test.jsx, TopicReview.test.jsx, and ProjectsHome.test.jsx with real tests that match the new inline edit behavior.

For TopicActions.test.jsx: import TopicCard component directly. Create mock topic with avatars. Render with renderWithProviders. Test that Pencil click (for non-approved topic) enters edit mode (check for input elements). Test Save button calls the mutation functions. Test Cancel resets to read-only view.

For TopicReview.test.jsx: render TopicReview page with mock topics data. Test that status filter dropdown filters visible cards. Test that approve/reject buttons trigger confirmation dialog. Test that edit button does NOT open a SidePanel (data-testid='side-panel' should not appear on edit click).

For ProjectsHome.test.jsx: add test case for research_failed status — mock a project with `status: 'research_failed'`, verify a retry button appears in the rendered card. Also add test for the existing passing tests not breaking (the 4 currently-passing tests must still pass).

Run `cd dashboard && npm test -- --reporter=verbose --run 2>&1 | tail -30` after writing tests to confirm RED state before implementation.

Note: DO NOT make the tests pass in this task — they should be RED (failing) because the implementation doesn't exist yet. This task only replaces the meaningless `expect(true).toBe(false)` stubs with real test assertions.
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npm test -- --reporter=verbose --run 2>&1 | grep -E "FAIL|PASS|Tests:" | tail -10</automated>
  </verify>
  <done>
    - TopicActions.test.jsx has real test assertions (no `expect(true).toBe(false)` stubs)
    - TopicReview.test.jsx has real test assertions
    - ProjectsHome.test.jsx has retry button test assertion added
    - Tests are RED (failing) for inline edit tests — this is expected at Wave 0
    - Existing passing ProjectsHome tests (renders cards, empty state, modal, skeleton) still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inline edit in TopicCard and fix webhook hooks</name>
  <files>
    dashboard/src/components/topics/TopicCard.jsx
    dashboard/src/hooks/useTopics.js
    dashboard/src/hooks/useProjects.js
    dashboard/src/components/projects/ProjectCard.jsx
    dashboard/src/pages/TopicReview.jsx
  </files>
  <behavior>
    - TopicCard in edit mode: expanded section shows `<input>` for seo_title, `<textarea>` for narrative_hook and key_segments, `<input>` for each of 10 avatar fields
    - TopicCard in edit mode: Save and Cancel buttons visible at bottom of expanded section
    - TopicCard edit mode loading: inputs disabled + spinner on Save button while isSaving
    - useEditTopic: mutationFn calls webhookCall('topics/action', { action: 'edit', topic_id, fields })
    - useEditAvatar: mutationFn calls webhookCall('topics/action', { action: 'edit_avatar', topic_id, fields })
    - useRetryResearch: mutationFn calls webhookCall('project/create', { project_id })
    - ProjectCard: shows RefreshCw Retry button when status === 'research_failed', button stopPropagation to avoid NavLink navigation
    - TopicReview: passes onSave={editMutation.mutate} and onSaveAvatar={editAvatarMutation.mutate} to each TopicCard; no longer renders EditPanel SidePanel; no longer has handleEdit opening panelType='edit'
  </behavior>
  <action>
Read design-system/MASTER.md for input/button class patterns before implementing UI. All new inputs must use the same class pattern as EditPanel.jsx (`inputClass` pattern).

**TopicCard.jsx changes:**
1. Add `onSave` and `onSaveAvatar` props to component signature (alongside existing `onApprove`, `onReject`, `onRefine`, `onEdit`)
2. Add state: `isEditing`, `isSaving`, `editTopicFields`, `editAvatarFields`
3. Change Pencil button `onClick`: instead of `onEdit(topic)`, set `isEditing=true` and initialize editTopicFields/editAvatarFields from current topic data
4. In the expanded section (`expanded && ...`), conditionally render:
   - When `isEditing=false`: existing read-only display (narrative_hook, key_segments, avatar grid)
   - When `isEditing=true`: controlled inputs for `seo_title` (input), `narrative_hook` (textarea), `key_segments` (textarea), plus 10 avatar inputs; Save + Cancel buttons at bottom
5. Save handler: `Promise.all([onSave({topic_id, fields: editTopicFields}), onSaveAvatar({topic_id, fields: editAvatarFields})])` → set isSaving during call, setIsEditing(false) on success
6. Cancel handler: `setIsEditing(false)` (no save)
7. When `isEditing=true`, prevent the card header click from collapsing (ensure expanded stays true during edit)

**useTopics.js changes:**
1. Fix `useEditTopic`: change `webhookCall('topics/edit', ...)` to `webhookCall('topics/action', { action: 'edit', topic_id, fields })`
2. Add `useEditAvatar` export: same structure as `useEditTopic` but calls `webhookCall('topics/action', { action: 'edit_avatar', topic_id, fields })`

**useProjects.js changes:**
1. Add `useRetryResearch` export: useMutation that calls `webhookCall('project/create', { project_id })`, on settled invalidates ['projects'] query

**ProjectCard.jsx changes:**
1. Add `onRetry` prop to component signature
2. When `status === 'research_failed'`: render a Retry button (RotateCcw or RefreshCw icon) inside the card — must use `e.stopPropagation()` to prevent NavLink navigation
3. Button class: small, amber-colored, positioned below the research_failed badge area

**TopicReview.jsx changes:**
1. Import `useEditAvatar` alongside existing hooks
2. Add `editAvatarMutation = useEditAvatar(projectId)`
3. Remove `panelType` state usage for 'edit' (keep for 'refine' only)
4. Update `handleEdit` to be a no-op or remove it — TopicCard now handles edit internally
5. Pass `onSave={(vars) => editMutation.mutateAsync(vars)}` and `onSaveAvatar={(vars) => editAvatarMutation.mutateAsync(vars)}` to each TopicCard
6. Remove `<EditPanel ...>` from render (keep `<RefinePanel>`)
7. Do NOT delete the EditPanel.jsx file — just stop importing/using it in TopicReview

After all changes, run `cd dashboard && npm test -- --reporter=verbose --run` to confirm tests go GREEN.

Build and deploy: `cd dashboard && npm run build && scp -r dist/. root@srv1297445.hstgr.cloud:/opt/dashboard/`
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npm test -- --reporter=verbose --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All TopicActions tests GREEN (inline edit mode working)
    - All TopicReview tests GREEN (edit no longer opens SidePanel)
    - All ProjectsHome tests GREEN including retry button test
    - `npm run build` completes without errors
    - Dashboard deployed to VPS
    - `useEditTopic` uses `topics/action` path (not `topics/edit`)
    - `useEditAvatar` exported from useTopics.js
    - `useRetryResearch` exported from useProjects.js
    - TopicReview no longer renders EditPanel SidePanel
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd dashboard && npm test -- --reporter=verbose --run` — all tests GREEN
2. `npm run build` — zero errors
3. `grep -n "topics/edit" dashboard/src/hooks/useTopics.js` — returns nothing (bug fixed)
4. `grep -n "topics/action" dashboard/src/hooks/useTopics.js` — returns 2+ matches (edit + edit_avatar)
5. `grep -n "useRetryResearch" dashboard/src/hooks/useProjects.js` — returns the function
6. Manual spot-check in browser: open a TopicCard, click Pencil, verify inputs appear; click Cancel, verify read-only view returns
</verification>

<success_criteria>
- All dashboard tests pass: `npm test -- --run` exits 0
- TopicCard shows inline edit mode (no SidePanel) when Pencil clicked on non-approved topic
- Save calls webhookCall('topics/action') with action='edit' and action='edit_avatar' separately
- Cancel exits edit mode without saving
- ProjectCard shows Retry button when status=research_failed
- Retry calls /webhook/project/create with existing project_id
- Dashboard deployed to VPS and serving latest build
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-agent-workflows/09-04-SUMMARY.md`
</output>
