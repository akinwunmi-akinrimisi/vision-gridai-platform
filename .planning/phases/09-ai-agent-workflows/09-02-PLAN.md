---
phase: 09-ai-agent-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/WF_TOPICS_GENERATE.json
autonomous: true
requirements:
  - AGNT-04
  - AGNT-06
  - AGNT-07
  - AGNT-08
  - AGNT-09

must_haves:
  truths:
    - "Triggering topic generation when topics already exist returns { topics_exist: true, count: N } without calling Claude"
    - "Triggering with force=true on a project with generating_topics status completes the partial batch (not restart)"
    - "Claude topic generation API call uses niche_system_prompt from projects table as the system parameter (not substring hack)"
    - "All existing topic titles + hooks are passed to Claude as dedup context on any generation"
    - "production_log entries written at workflow start and end"
    - "Claude API failure writes topics.status=failed and error_log, then stops gracefully"
  artifacts:
    - path: "workflows/WF_TOPICS_GENERATE.json"
      provides: "Hardened topic + avatar generation workflow"
      contains: "niche_system_prompt"
  key_links:
    - from: "Check Existing Topics node (HTTP GET topics count)"
      to: "Idempotency Code node"
      via: "count of returned rows"
      pattern: "topics_exist"
    - from: "wf-tg-anthropic node system parameter"
      to: "projects.niche_system_prompt"
      via: "Read Project node result"
      pattern: "niche_system_prompt"
    - from: "Claude API call error output"
      to: "PATCH topics status=failed"
      via: "onError: continueRegularOutput + IF isError"
      pattern: "generating_failed"
---

<objective>
Harden WF_TOPICS_GENERATE: add idempotency guard (return topics_exist flag, support force=true additive batches, detect and complete partial batches), fix system prompt loading (load from projects.niche_system_prompt instead of substring hack), add existing-topics dedup context, add production_log entries, and connect error handlers on all failure branches.

Purpose: AGNT-04 (robust topic generation), AGNT-06 (idempotency), AGNT-07 (production_log), AGNT-08 (error handling), AGNT-09 (system prompt fix) are all unmet in the current WF_TOPICS_GENERATE. The substring hack `$('Build Prompt').first().json.prompt.substring(0,500)` truncates the system prompt to 500 characters. There are zero error handlers. Re-triggering creates duplicate topics. No production_log writes exist.

Output: Updated workflows/WF_TOPICS_GENERATE.json deployed to n8n (ID: J5NTvfweZRiKJ9fG) and activated.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-ai-agent-workflows/09-CONTEXT.md
@.planning/phases/09-ai-agent-workflows/09-RESEARCH.md

<!-- WF_TOPICS_GENERATE n8n ID: J5NTvfweZRiKJ9fG -->
<!-- Workflow has nodes: Validate, Read Project, Build Prompt, wf-tg-anthropic (Claude API call), Split/Insert topics, Insert avatars -->
<!-- System prompt fix: node wf-tg-anthropic jsonBody currently has system: $('Build Prompt').first().json.prompt.substring(0,500) -->
<!-- Fix to: system: $('Read Project').first().json[0].niche_system_prompt -->
<!-- user message content stays as-is (the topic_generator prompt from prompt_configs) -->

<interfaces>
<!-- Idempotency Code node logic (from RESEARCH.md Pattern 3) -->
<!--
const body = $('Validate').first().json;
const project_id = body.project_id;
const force = body.force === true || body.force === 'true';

const topicsResult = $('Check Existing Topics').first().json;
const existingTopics = Array.isArray(topicsResult) ? topicsResult : [];
const existingCount = existingTopics.length;

const project = $('Read Project').first().json[0] || {};
const isCrashResume = force && project.status === 'generating_topics';

if (existingCount > 0 && !force) {
  return [{ json: { topics_exist: true, count: existingCount, project_id, __early_return: true } }];
}

let startTopicNumber, topicsNeeded;
if (isCrashResume && existingCount % 25 !== 0) {
  startTopicNumber = existingCount + 1;
  topicsNeeded = 25 - (existingCount % 25);
} else {
  startTopicNumber = existingCount + 1;
  topicsNeeded = 25;
}

const existingTitles = existingTopics.map(t => ({
  topic_number: t.topic_number,
  seo_title: t.seo_title,
  narrative_hook: t.narrative_hook
}));

return [{ json: { project_id, force, isCrashResume, existingCount, startTopicNumber, topicsNeeded, existingTitles, __early_return: false } }];
-->

<!-- Check Existing Topics HTTP Request (added before idempotency Code node) -->
<!-- GET {{$env.SUPABASE_URL}}/rest/v1/topics?project_id=eq.{{$json.project_id}}&select=topic_number,seo_title,narrative_hook&order=topic_number.asc -->
<!-- Headers: apikey + Authorization Bearer SUPABASE_SERVICE_ROLE_KEY -->

<!-- Early return wiring -->
<!-- IF __early_return === true → "Respond Topics Exist" node: { topics_exist: true, count: N } -->
<!-- IF __early_return === false → continue to "Status → Generating" + Claude call -->

<!-- Error handler pattern for Claude call -->
<!-- onError: 'continueRegularOutput' on wf-tg-anthropic node -->
<!-- Code node "Check Claude Error": check response.error, return { isError: true, error_message } -->
<!-- IF node: isError → PATCH topics status=generating_failed + production_log failed -->

<!-- production_log format -->
<!-- started: { project_id, topic_id: null, stage: 'topic_generation', action: 'started', details: { topicsNeeded } } -->
<!-- completed: { ..., action: 'completed', details: { topics_inserted: 25, avatars_inserted: 25 } } -->
<!-- failed: { ..., action: 'failed', details: { error: error_message } } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix system prompt, add idempotency guard and dedup context</name>
  <files>workflows/WF_TOPICS_GENERATE.json</files>
  <action>
SSH to VPS. Fetch current live workflow JSON:
```
curl -s -H "Authorization: Bearer {N8N_API_KEY}" \
  https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/J5NTvfweZRiKJ9fG \
  > /tmp/wf_topics_generate_live.json
```

**Fix 1 — System prompt (AGNT-09):**
Find node `wf-tg-anthropic` in nodes array. Locate the `system` field in `parameters.jsonBody` or `parameters.body`. Change from:
```
$('Build Prompt').first().json.prompt.substring(0, 500)
```
to:
```
$('Read Project').first().json[0].niche_system_prompt
```
This is a single expression change in the node's JSON configuration.

**Fix 2 — Idempotency guard (AGNT-06):**
Insert these nodes before the existing fan-out (before "Status → Generating" and "Respond" nodes):

1. **"Check Existing Topics"** (HTTP Request GET):
   - URL: `{{$env.SUPABASE_URL}}/rest/v1/topics?project_id=eq.{{$('Read Project').first().json[0].id}}&select=topic_number,seo_title,narrative_hook&order=topic_number.asc`
   - Response: array of existing topics

2. **"Idempotency Guard"** (Code node):
   Full logic from interfaces block above. Outputs either `{ __early_return: true, topics_exist: true, count: N }` or `{ __early_return: false, startTopicNumber, topicsNeeded, existingTitles, ... }`

3. **"IF Early Return"** (IF node):
   - Condition: `{{ $json.__early_return }}` equals true
   - True → "Respond Topics Exist" node
   - False → continue to production flow

4. **"Respond Topics Exist"** (Respond to Webhook):
   - Response body: `{ "topics_exist": true, "count": "{{$json.count}}" }`
   - HTTP status: 200

**Fix 3 — Dedup context injection:**
After "Idempotency Guard" (false branch), the existingTitles array is available at `$json.existingTitles`. This needs to flow into the Claude API call's user message. Modify the Build Prompt node (or add a "Build Dedup Context" Code node) to append to the user message:
```
If existingTitles.length > 0, append to the user message:
"\n\nEXISTING TOPICS — avoid overlap with these:\n" + JSON.stringify(existingTitles)
```

Also inject `startTopicNumber` and `topicsNeeded` into the prompt so Claude generates the right quantity starting from the right number.

After all modifications, write updated JSON to `/tmp/wf_topics_generate_updated.json`. Validate JSON, deploy via PUT, activate.

Copy deployed JSON back to `workflows/WF_TOPICS_GENERATE.json`.
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform && python3 -c "
import json
wf = json.load(open('workflows/WF_TOPICS_GENERATE.json'))
nodes = {n['name']: n for n in wf['nodes']}
assert 'Check Existing Topics' in nodes, 'Missing Check Existing Topics node'
assert 'Idempotency Guard' in nodes, 'Missing Idempotency Guard node'
assert 'IF Early Return' in nodes, 'Missing IF Early Return node'
assert 'Respond Topics Exist' in nodes, 'Missing Respond Topics Exist node'
# Check system prompt fix
claude_node = nodes.get('wf-tg-anthropic', {})
node_str = json.dumps(claude_node)
assert 'substring' not in node_str, 'substring hack still present'
assert 'niche_system_prompt' in node_str, 'niche_system_prompt not in Claude node'
print('PASS: idempotency nodes present, system prompt fixed')
"</automated>
  </verify>
  <done>
    - `wf-tg-anthropic` node uses `niche_system_prompt` not `substring(0,500)` for system parameter
    - `Check Existing Topics` HTTP GET node fetches existing topics before Claude call
    - `Idempotency Guard` Code node returns early with `topics_exist:true` when appropriate
    - `IF Early Return` routes to `Respond Topics Exist` on early return
    - Existing topics titles+hooks passed as dedup context to Claude
    - `startTopicNumber` and `topicsNeeded` available for Claude prompt injection
    - Workflow deployed and active in n8n
  </done>
</task>

<task type="auto">
  <name>Task 2: Add production_log nodes and error handlers</name>
  <files>workflows/WF_TOPICS_GENERATE.json</files>
  <action>
Continuing from Task 1's deployed state. Fetch updated live JSON, apply additional modifications.

**Production log nodes (AGNT-07):**

1. **"Log Started"** (HTTP Request POST production_log):
   - Position: immediately after "Validate" node, before "Read Project"
   - URL: `{{$env.SUPABASE_URL}}/rest/v1/production_log`
   - Body: `{ "project_id": "{{$json.project_id}}", "topic_id": null, "stage": "topic_generation", "action": "started", "details": {} }`

2. **"Log Completed"** (HTTP Request POST production_log):
   - Position: after all avatars are inserted (final success node)
   - Body: `{ "project_id": "{{$('Validate').first().json.project_id}}", "topic_id": null, "stage": "topic_generation", "action": "completed", "details": { "topics_inserted": 25, "avatars_inserted": 25 } }`

**Error handlers (AGNT-08):**

For node `wf-tg-anthropic`:
1. Set `"onError": "continueRegularOutput"`
2. Add **"Check Claude Error"** (Code node):
   ```javascript
   const response = $input.first().json;
   if (response.error || response.type === 'error') {
     return [{ json: {
       isError: true,
       error_message: response.error?.message || JSON.stringify(response.error || response),
       project_id: $('Validate').first().json.project_id
     }}];
   }
   return [{ json: { ...response, isError: false } }];
   ```
3. Add **"IF Generation Error"** (IF node): `{{ $json.isError }}` true → error path
4. Error path:
   - **"PATCH Generation Failed"**: PATCH projects `{ "status": "generating_failed", "error_log": "{{$json.error_message}}" }` where `id=eq.{{$json.project_id}}`
   - **"Log Generation Failed"**: POST production_log `{ "project_id": "{{$json.project_id}}", "topic_id": null, "stage": "topic_generation", "action": "failed", "details": { "error": "{{$json.error_message}}" } }`

For Supabase INSERT failures (topics insert and avatars insert nodes):
1. Add `onError: 'continueRegularOutput'` to each INSERT HTTP Request node
2. Add downstream Code nodes to check for non-2xx responses
3. Route failures to PATCH project status=failed + Log Failed

Deploy final updated JSON via PUT. Activate. Sync to local `workflows/WF_TOPICS_GENERATE.json`.
  </action>
  <verify>
    <automated>cd /c/Users/DELL/Documents/Antigravity/vision-gridai-platform && python3 -c "
import json
wf = json.load(open('workflows/WF_TOPICS_GENERATE.json'))
nodes = {n['name']: n for n in wf['nodes']}
assert 'Log Started' in nodes, 'Missing Log Started node'
assert 'Log Completed' in nodes, 'Missing Log Completed node'
assert 'Log Generation Failed' in nodes, 'Missing Log Generation Failed node'
assert 'Check Claude Error' in nodes, 'Missing Check Claude Error node'
assert 'IF Generation Error' in nodes, 'Missing IF Generation Error node'
assert 'PATCH Generation Failed' in nodes, 'Missing PATCH Generation Failed node'
print('PASS: all production_log and error handler nodes present')
"</automated>
  </verify>
  <done>
    - `Log Started` POST node fires immediately after Validate
    - `Log Completed` POST node fires after all inserts succeed
    - `Log Generation Failed` POST node fires on any Claude or Supabase error
    - `wf-tg-anthropic` has `onError: continueRegularOutput`
    - `Check Claude Error` + `IF Generation Error` routes errors to PATCH + Log
    - `PATCH Generation Failed` writes project `status=generating_failed` and `error_log`
    - Workflow deployed, active, and local JSON synced
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `python3 -c "import json; wf=json.load(open('workflows/WF_TOPICS_GENERATE.json')); print([n['name'] for n in wf['nodes']])"` — verify all new nodes present
2. `grep -c "niche_system_prompt" workflows/WF_TOPICS_GENERATE.json` returns >= 1
3. `grep -c "substring" workflows/WF_TOPICS_GENERATE.json` returns 0
4. Workflow active via: `curl -s -H "Authorization: Bearer {KEY}" https://n8n.srv1297445.hstgr.cloud/api/v1/workflows/J5NTvfweZRiKJ9fG | python3 -c "import sys,json; d=json.load(sys.stdin); print('active:', d.get('active'))"`
</verification>

<success_criteria>
- WF_TOPICS_GENERATE deployed and active with all hardening
- Second trigger without force returns `{ topics_exist: true, count: N }` immediately
- force=true with generating_topics status completes partial batch
- Claude system parameter is full niche_system_prompt (no 500-char truncation)
- production_log entries appear in ProductionMonitor when workflow runs
- Any Claude failure writes project status=generating_failed
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-agent-workflows/09-02-SUMMARY.md`
</output>
