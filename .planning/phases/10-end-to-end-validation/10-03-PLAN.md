---
phase: 10-end-to-end-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified: []
autonomous: false
requirements:
  - E2E-01

must_haves:
  truths:
    - "US Credit Cards project created and niche research completes — niche_profiles row exists"
    - "25 topics and 25 avatars generated with all required fields"
    - "Gate 1 actions (approve, reject, refine) all update topic status in real-time on dashboard"
    - "All 6 Phase 9 human-verification items confirmed working on live system"
  artifacts:
    - path: "Supabase: projects table"
      provides: "Project record with status=ready_for_topics, niche_system_prompt populated"
      contains: "niche_system_prompt IS NOT NULL"
    - path: "Supabase: topics table"
      provides: "25 topics with review_status values (approved/rejected/refined)"
      contains: "count=25, at least 1 approved"
    - path: "Supabase: niche_profiles table"
      provides: "Research results from WF_PROJECT_CREATE"
      contains: "1 row for project"
  key_links:
    - from: "Dashboard ProjectsHome New Project button"
      to: "WF_WEBHOOK_PROJECT_CREATE → WF_PROJECT_CREATE"
      via: "POST /webhook/project/create"
      pattern: "project/create"
    - from: "Dashboard NicheResearch Generate Topics button"
      to: "WF_TOPICS_GENERATE"
      via: "POST /webhook/topics/generate"
      pattern: "topics/generate"
    - from: "Topic approve/reject/refine buttons"
      to: "WF_TOPICS_ACTION"
      via: "POST /webhook/topics/action"
      pattern: "topics/action"
---

<objective>
Run the live E2E test for Stages 1 and 2: project creation, niche research, topic generation, and Gate 1 review. Also verify all 6 Phase 9 human-verification items during this run.

Purpose: This is the first live test run of the full agent pipeline with real API calls. Phase 9 automated tests verified code was correct; this confirms it works in production.
Output: Bug log documenting any issues found, all Phase 9 human items checked off, at least 1 topic approved and ready for script generation.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-ai-agent-workflows/09-VERIFICATION.md
@.planning/phases/10-end-to-end-validation/10-RESEARCH.md

<interfaces>
VPS SSH: root@srv1297445.hstgr.cloud
Dashboard URL: https://dashboard.operscale.cloud
n8n URL: https://n8n.srv1297445.hstgr.cloud
Supabase: https://supabase.operscale.cloud

Supabase verification queries:
- SELECT status, niche_system_prompt IS NOT NULL FROM projects WHERE niche='US Credit Cards';
- SELECT count(*) FROM niche_profiles WHERE project_id='PROJECT_ID';
- SELECT count(*),prompt_type FROM prompt_configs WHERE project_id='PROJECT_ID' AND is_active=true GROUP BY prompt_type;
- SELECT count(*) FROM topics WHERE project_id='PROJECT_ID';
- SELECT count(*) FROM avatars WHERE project_id='PROJECT_ID';

Phase 9 human-verification items (from 09-VERIFICATION.md):
1. New Project research flow (live Claude API + status transitions + niche_profiles row + 7 prompt_configs)
2. Re-trigger research idempotency (skip Claude web search, version increment in prompt_configs)
3. topics_exist confirm dialog (shows count, force=true re-sends, additive batch appears)
4. Inline edit Save (both PATCH calls fire, card exits edit mode with updated values)
5. Error handler path (bad API key → research_failed badge + production_log entry)
6. Retry Research button (click changes status back to researching)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight checks before E2E run</name>
  <what-built>
Wave 1 workflows (WF_SCRIPT_GENERATE, WF_SCRIPT_APPROVE, WF_SCRIPT_REJECT) built and active. Dashboard deployed. All 18 production workflows active.
  </what-built>
  <how-to-verify>
Run these checks before starting the E2E run:

1. Verify all 3 new script workflows are active on n8n:
   - Visit https://n8n.srv1297445.hstgr.cloud/workflows
   - Search for "SCRIPT" — should see WF_SCRIPT_GENERATE, WF_SCRIPT_APPROVE, WF_SCRIPT_REJECT all active

2. Verify dashboard is accessible:
   - Visit https://dashboard.operscale.cloud
   - Should load without errors, see Projects Home

3. Verify dashboard Supabase connection:
   - Open browser DevTools (Network tab)
   - Look for requests to supabase.operscale.cloud — should return 200, not 401
   - If 401: dashboard/.env has wrong anon key. Fix it: add real VITE_SUPABASE_ANON_KEY to dashboard/.env, run npm run build, scp dist to VPS

4. Check Google Drive OAuth is still valid:
   - In n8n: Settings → Credentials → GoogleDriveAccount → Test connection
   - If expired, re-authorize before production pipeline run

5. Check Kie.ai account balance:
   - Log in to kie.ai dashboard
   - If balance < $20, top up (1 video = ~$15.33 for media)
   - Note: Can run E2E without video clips if budget is low — stop after TTS+images to verify pipeline (test Gate 3 with a partial video is acceptable for E2E-01)
  </how-to-verify>
  <resume-signal>Type "pre-flight OK" when all checks pass, or describe any issues to fix first</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stage 1 — Create US Credit Cards project and verify research</name>
  <what-built>
WF_PROJECT_CREATE is deployed and hardened (Phase 9). It calls Claude with web_search tool, writes niche_profiles, generates 7 prompt_configs.
  </what-built>
  <how-to-verify>
This verifies E2E Stage 1 and Phase 9 human-verification item #1.

STEP 1: Create the project
- On dashboard Projects Home, click "+ New Project"
- Name: "US Credit Cards Channel"
- Niche: "US Credit Cards"
- Click "Start Research"

STEP 2: Monitor progress (Supabase Realtime should update ProjectCard status)
- Watch ProjectCard status transitions: created → researching → ready_for_topics
- Total expected time: 3-8 minutes (Claude web search + prompt generation)
- If stuck at "researching" for >10 minutes: check n8n execution log for WF_PROJECT_CREATE

STEP 3: Verify Supabase state after completion
Run these via Supabase Dashboard SQL editor or psql:
```sql
SELECT id, status, niche_system_prompt IS NOT NULL AS has_system_prompt, niche_expertise_profile IS NOT NULL AS has_profile
FROM projects WHERE niche = 'US Credit Cards';
-- Expected: status='ready_for_topics', both true

SELECT count(*) FROM niche_profiles WHERE project_id = 'PROJECT_ID_FROM_ABOVE';
-- Expected: 1

SELECT count(*), prompt_type FROM prompt_configs
WHERE project_id = 'PROJECT_ID' AND is_active = true
GROUP BY prompt_type ORDER BY prompt_type;
-- Expected: 7 rows (evaluator, script_pass1, script_pass2, script_pass3, system_prompt, topic_generator, visual_director)
```

STEP 4: Verify idempotency (Phase 9 item #2)
- On NicheResearch page, click "Re-research" button (or navigate to project and re-trigger)
- Expected: n8n execution completes faster (no web search), prompt_configs version increments
- In Supabase: SELECT max(version), count(*) FROM prompt_configs WHERE project_id='...' AND is_active=true — version should be 2

STEP 5: Note the project_id for use in subsequent tasks.

LOG any bugs found:
- Bug format: [STAGE] [SEVERITY: P0/P1/P2] Description. Expected: X. Actual: Y. n8n execution URL if applicable.
  </how-to-verify>
  <resume-signal>Type "stage-1 OK [PROJECT_ID]" with the actual project UUID, or describe bugs found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Stage 2 — Generate topics and run Gate 1</name>
  <what-built>
WF_TOPICS_GENERATE is deployed and hardened. WF_TOPICS_ACTION handles approve/reject/refine/edit/edit_avatar.
  </what-built>
  <how-to-verify>
This verifies E2E Stage 2 and Phase 9 human-verification items #3, #4.

STEP 1: Generate 25 topics
- On NicheResearch page for the US Credit Cards project, click "Generate Topics"
- Expected: 25 topics appear on Topic Review page (takes 2-4 minutes)
- If no topics appear after 5 minutes: check n8n execution log for WF_TOPICS_GENERATE

STEP 2: Verify Supabase state
```sql
SELECT count(*) FROM topics WHERE project_id = 'PROJECT_ID';
-- Expected: 25

SELECT count(*) FROM avatars WHERE project_id = 'PROJECT_ID';
-- Expected: 25

SELECT topic_number, seo_title, review_status FROM topics
WHERE project_id = 'PROJECT_ID' ORDER BY topic_number LIMIT 5;
-- Expected: all review_status='pending', seo_title non-null
```

STEP 3: Test idempotency confirm dialog (Phase 9 item #3)
- Click "Generate Topics" again on the same project
- Expected: ConfirmDialog appears showing "25 topics already exist. Generate 25 more?"
- Click "Generate 25 More" — verify topics count increases to 50 in Supabase
- These extra topics are fine to keep or clean up (DELETE FROM topics WHERE topic_number > 25 AND project_id='...')

STEP 4: Gate 1 — Mix of approve/reject/refine actions
Perform ALL of these to test the full Gate 1 flow:
- Approve at least 5 topics (individually or bulk select)
- Reject at least 2 topics (with feedback text)
- Refine at least 1 topic: click Refine, type an instruction like "Make the hook more confrontational, targeting millennials who distrust credit card companies", submit. Verify Claude regenerates the topic.
- After refinement: approve the refined topic

Verify in Supabase:
```sql
SELECT review_status, count(*) FROM topics WHERE project_id='PROJECT_ID' GROUP BY review_status;
-- Expected: approved >= 5, rejected >= 2, pending = rest
```

STEP 5: Test inline edit (Phase 9 item #4)
- Open one TopicCard, click the pencil icon
- Edit the SEO title and one avatar field (e.g., pain_point)
- Click Save
- Verify: card shows updated values without page reload
- Verify in Supabase: SELECT seo_title FROM topics WHERE id='...'; and avatar field updated

STEP 6: Verify error handler (Phase 9 item #5) — OPTIONAL, do only if time permits
- SSH to VPS, temporarily set wrong ANTHROPIC_API_KEY in n8n env
- Trigger a fresh project's research
- Verify projects.status='research_failed' appears in Supabase and dashboard shows error badge
- Restore correct API key, verify Retry button appears

STEP 7: Test Retry button (Phase 9 item #6)
- If you ran step 6: click Retry Research on the failed project, verify it transitions to researching
- If you skipped step 6: manually set a project status to research_failed in Supabase, verify Retry button appears in dashboard

LOG all bugs found using the same format as Task 2.
  </how-to-verify>
  <resume-signal>Type "stage-2 OK" with a note of how many topics approved/rejected/refined, or describe bugs found</resume-signal>
</task>

</tasks>

<verification>
Stage 1 complete:
- projects.status = 'ready_for_topics' for US Credit Cards project
- niche_profiles count = 1
- prompt_configs count = 7 active rows covering all required types

Stage 2 complete:
- topics count = 25
- avatars count = 25
- At least 1 topic with review_status = 'approved' (prerequisite for Stage 3)
- Gate 1 actions all verified: approve, reject, refine all work

Phase 9 items verified: 1, 2, 3, 4 confirmed. Items 5 and 6 confirmed or documented as deferred.

Bug log: Any P0/P1 bugs documented and ready for Plan 10-06 triage.
</verification>

<success_criteria>
- "US Credit Cards Channel" project fully researched and in ready_for_topics state
- 25 topics generated, at least 1 approved (needed for Stage 3)
- All Gate 1 dashboard interactions verified working (approve/reject/refine/inline-edit)
- Phase 9 human-verification items 1-4 confirmed live
- Bug log created (even if empty = perfect)
</success_criteria>

<output>
After completion, create .planning/phases/10-end-to-end-validation/10-03-SUMMARY.md with:
- Project UUID
- Topics approved count
- Bugs found (P0/P1/P2)
- Phase 9 items status
</output>
