---
phase: 10-end-to-end-validation
plan: 05
type: execute
wave: 2
depends_on:
  - 10-04
files_modified: []
autonomous: false
requirements:
  - E2E-01

must_haves:
  truths:
    - "TTS audio generated for all scenes (scenes.audio_status='uploaded' for all rows)"
    - "Images generated for ~100 scenes (scenes.image_status='uploaded')"
    - "Video clips generated for I2V and T2V scenes"
    - "Assembly completes and topics.drive_video_url is populated"
    - "Gate 3 (Video Review page) shows playable video and Approve button triggers YouTube upload"
  artifacts:
    - path: "Supabase: scenes table"
      provides: "All scenes with audio_status=uploaded, image_status/video_status populated"
      contains: "audio_status=uploaded for all"
    - path: "Supabase: topics table"
      provides: "drive_video_url and assembly_status=complete"
      contains: "drive_video_url IS NOT NULL"
  key_links:
    - from: "Production Monitor DotGrid"
      to: "scenes table Realtime updates"
      via: "Supabase Realtime subscription on scenes table"
      pattern: "audio_status"
    - from: "Gate 3 Approve button"
      to: "WF_WEBHOOK_PUBLISH → WF_YOUTUBE_UPLOAD"
      via: "POST /webhook/publish"
      pattern: "youtube_video_id"
---

<objective>
Run live E2E test for Stages 4-5: full production pipeline (TTS, images, I2V/T2V, assembly) and Gate 3 (Video Review and YouTube upload).

Purpose: Validates that all 12 production workflows work end-to-end with real external APIs (Google Cloud TTS, Kie.ai, FFmpeg, YouTube). This is the longest-running stage (~3-4 hours total).
Output: A published YouTube video from the US Credit Cards niche, confirming E2E-01 requirement complete.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-end-to-end-validation/10-RESEARCH.md
@.planning/phases/10-end-to-end-validation/10-04-SUMMARY.md

<interfaces>
Production pipeline self-chains:
WF_WEBHOOK_PRODUCTION → WF_TTS_AUDIO (all 172 scenes) → fires WF_IMAGE_GENERATION + WF_I2V_GENERATION + WF_T2V_GENERATION in parallel → WF_CAPTIONS_ASSEMBLY → WF_YOUTUBE_UPLOAD (only after Gate 3 approve)

Expected timing per stage:
- TTS audio (172 scenes): 15-20 minutes
- Images (100 Seedream 4.5): 20-30 minutes
- I2V clips (25 Kling 2.1): 30-45 minutes
- T2V clips (72 Kling 2.1): 60-90 minutes
- Assembly (FFmpeg): 5-10 minutes
Total: ~3-4 hours

Pitfall alerts from research:
- Google Drive OAuth may have expired — check before production run
- Chirp 3 HD TTS requires GCP_REGION=us-east5 — verify TTS credential is correct
- Kie.ai async poll pattern — tasks take 2-5 min each, do not interrupt

Monitoring queries:
- SELECT count(*), audio_status FROM scenes WHERE topic_id='TOPIC_ID' GROUP BY audio_status;
- SELECT count(*), image_status FROM scenes WHERE topic_id='TOPIC_ID' GROUP BY image_status;
- SELECT audio_progress, images_progress, i2v_progress, t2v_progress, assembly_status FROM topics WHERE id='TOPIC_ID';
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stage 4 — Monitor production pipeline (TTS → images → video)</name>
  <what-built>
All production workflows (WF_TTS_AUDIO, WF_IMAGE_GENERATION, WF_I2V_GENERATION, WF_T2V_GENERATION, WF_CAPTIONS_ASSEMBLY) are deployed and active from Phase 8. Production was triggered at end of Plan 10-04.
  </what-built>
  <how-to-verify>
NOTE: This stage takes 3-4 hours. Monitor periodically, not continuously.

STEP 1: Verify TTS started
- Navigate to Production Monitor page for the US Credit Cards project
- DotGrid should be filling in as scenes complete audio (● = complete, ○ = pending)
- Expected: All 172 dots fill in within 15-20 minutes
- If no dots after 10 minutes: check n8n WF_TTS_AUDIO execution log

Check Supabase progress:
```sql
SELECT count(*), audio_status FROM scenes WHERE topic_id='TOPIC_ID' GROUP BY audio_status;
-- While running: mix of pending/uploaded
-- Complete: all 172 = uploaded
```

STEP 2: Verify TTS → image/video chain fires
After TTS completes (all 172 audio scenes uploaded), the workflow should fire:
- WF_IMAGE_GENERATION (for ~100 static_image scenes)
- WF_I2V_GENERATION (for ~25 i2v scenes)
- WF_T2V_GENERATION (for ~72 t2v scenes)

These run in parallel. Monitor in Production Monitor page and n8n execution list.

STEP 3: If TTS fails mid-run
If TTS stops before all scenes are complete:
- Check n8n execution log for error details
- Common issues: Google TTS 401 (OAuth expired) → re-authorize credential → retry
- Supervisor agent (WF_SUPERVISOR) runs every 30 minutes and retries stalled workflows automatically

STEP 4: Monitor image + video generation (takes 1-2 hours)
Periodically check Supabase:
```sql
SELECT audio_progress, images_progress, i2v_progress, t2v_progress FROM topics WHERE id='TOPIC_ID';
-- Expected format: "done:47/172" or "complete"
```

STEP 5: Verify FFmpeg assembly after all media is ready
- Assembly auto-triggers after all images + clips are ready
- Check topics.assembly_status = 'complete'
- Check topics.drive_video_url IS NOT NULL

STEP 6: Verify Production Monitor DotGrid activity log
- Activity log should show production_log entries in real-time
- Entries: started, scene completions, stage transitions

LOG any bugs:
- [PROD] [P0] TTS failed at scene N — error: X
- [PROD] [P1] Images generation stuck — scenes with image_status=pending after 45 minutes
- [PROD] [P2] DotGrid not updating — Realtime subscription issue
  </how-to-verify>
  <resume-signal>Type "production OK" when assembly is complete and drive_video_url is set, or describe which stage failed and the error details</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate 3 — Review video and approve for YouTube publish</name>
  <what-built>
WF_CAPTIONS_ASSEMBLY has produced the final video. WF_YOUTUBE_UPLOAD is deployed. WF_WEBHOOK_PUBLISH chains Gate 3 approval to YouTube upload.
  </what-built>
  <how-to-verify>
STEP 1: Navigate to Video Review page (Gate 3)
- From Project Dashboard, find the topic with assembly_status=complete
- Click "Review Video" or navigate to the Video Review page for that topic
- Expected: Embedded video player showing the assembled video (Google Drive URL)
- Expected: YouTube metadata pre-populated (title, description, tags from script_metadata)
- Expected: Total production cost displayed

STEP 2: Review the video
- Play at least 2-3 minutes of the video
- Check: Audio is present, images match narration, video clips play at correct timing
- If video player fails to load: check topics.drive_video_url is a valid shareable Google Drive link

STEP 3: Edit metadata if needed (optional)
- Inline edit the YouTube title or description if desired
- Tags should auto-populate from script_metadata.video_metadata.tags

STEP 4: Approve and publish
- Click "Approve & Publish"
- Expected: WF_WEBHOOK_PUBLISH fires, then WF_YOUTUBE_UPLOAD
- Expected: topics.youtube_video_id populated within 2-5 minutes
- Expected: topics.youtube_url set
- Expected: Production Monitor shows YouTube upload progress

STEP 5: Verify in Supabase
```sql
SELECT youtube_url, youtube_video_id, published_at FROM topics WHERE id = 'TOPIC_ID';
-- Expected: youtube_url IS NOT NULL, youtube_video_id IS NOT NULL, published_at IS NOT NULL
```

STEP 6: Verify YouTube
- Visit the YouTube URL from Supabase
- Video should be visible (may be private/unlisted depending on settings)
- If YouTube API quota exceeded (10,000 units/day): video upload will fail with 403
  - Workaround: approve for next day, or schedule publish for tomorrow

STEP 7: Final E2E confirmation
The E2E-01 requirement is SATISFIED when ALL of these are true:
1. US Credit Cards project has niche research complete
2. 25 topics generated and Gate 1 actions tested
3. At least one topic has script_quality_score set and scenes populated
4. Gate 2 approved and production triggered
5. Production pipeline (TTS + images + video + assembly) completed
6. Gate 3 reviewed and YouTube upload completed (or initiated)

LOG any final bugs. Summarize E2E-01 pass/fail verdict.
  </how-to-verify>
  <resume-signal>Type "E2E COMPLETE [YOUTUBE_URL]" with the published video URL, or describe what prevented completion</resume-signal>
</task>

</tasks>

<verification>
Production pipeline complete:
- scenes: all audio_status=uploaded (172 rows)
- scenes: image_status=uploaded for static_image scenes
- scenes: video_status=uploaded for i2v/t2v scenes
- topics: assembly_status=complete
- topics: drive_video_url IS NOT NULL

Gate 3 complete:
- topics: youtube_video_id IS NOT NULL
- topics: youtube_url IS NOT NULL
- topics: published_at IS NOT NULL

E2E-01 requirement: SATISFIED
All 4 success criteria from ROADMAP Phase 10 are met:
1. US Credit Cards project completed niche research
2. 25 topics passed Gate 1 (mix of approve/reject/refine)
3. At least one topic completed script generation, passed Gate 2, entered production
4. Production pipeline completed, video reviewable at Gate 3

Analytics (WF_ANALYTICS_CRON) will pull YouTube stats within 24 hours automatically.
</verification>

<success_criteria>
- Full E2E pipeline completed for the US Credit Cards niche
- At least 1 video published to YouTube
- Production Monitor DotGrid, PassTracker, and activity log all worked via Supabase Realtime
- Bug log complete for Plan 10-06 triage
- E2E-01 requirement marked SATISFIED in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create .planning/phases/10-end-to-end-validation/10-05-SUMMARY.md with:
- YouTube URL of published video
- Total production cost
- Any bugs found (P0/P1/P2)
- E2E-01 pass/fail verdict
- Consolidated bug log from Plans 10-03, 10-04, 10-05
</output>
