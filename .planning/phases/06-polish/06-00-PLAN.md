---
phase: 06-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/src/__tests__/Settings.test.jsx
  - dashboard/src/hooks/useProjectSettings.js
  - dashboard/src/hooks/usePromptConfigs.js
  - dashboard/src/lib/settingsApi.js
  - dashboard/src/components/settings/ConfigTab.jsx
  - dashboard/src/components/settings/PromptsTab.jsx
  - dashboard/src/components/settings/PromptCard.jsx
autonomous: true
requirements:
  - OPS-03
  - OPS-04

must_haves:
  truths:
    - "Test file exists with failing tests covering OPS-03 and OPS-04 behaviors"
    - "All component and hook stubs exist so Vite resolves imports"
  artifacts:
    - path: "dashboard/src/__tests__/Settings.test.jsx"
      provides: "Test scaffolds for Settings page"
      min_lines: 80
    - path: "dashboard/src/hooks/useProjectSettings.js"
      provides: "Stub hook exporting useProjectSettings"
    - path: "dashboard/src/hooks/usePromptConfigs.js"
      provides: "Stub hook exporting usePromptConfigs and usePromptMutations"
    - path: "dashboard/src/lib/settingsApi.js"
      provides: "Stub API helpers"
    - path: "dashboard/src/components/settings/ConfigTab.jsx"
      provides: "Stub ConfigTab component"
    - path: "dashboard/src/components/settings/PromptsTab.jsx"
      provides: "Stub PromptsTab component"
    - path: "dashboard/src/components/settings/PromptCard.jsx"
      provides: "Stub PromptCard component"
  key_links: []
---

<objective>
Create Vitest test scaffolds and stub files for all Phase 6 Settings components, hooks, and API helpers.

Purpose: Wave 0 ensures RED-phase tests exist before implementation, and all import paths resolve so later plans can implement against defined contracts.
Output: 1 test file, 3 stub hooks/API files, 3 stub component files
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-CONTEXT.md
@.planning/phases/06-polish/06-RESEARCH.md
@.planning/phases/06-polish/06-VALIDATION.md

<interfaces>
<!-- Existing patterns the executor needs -->

From dashboard/src/lib/api.js:
```javascript
export async function webhookCall(endpoint, data = {}) { ... }
```

From dashboard/src/hooks/useRealtimeSubscription.js:
```javascript
export function useRealtimeSubscription(table, filter, queryKeys) { ... }
```

From dashboard/src/lib/supabase.js:
```javascript
export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

From dashboard/src/components/ui/ConfirmDialog.jsx:
```javascript
export default function ConfirmDialog({ isOpen, onClose, onConfirm, title, message, confirmText, confirmVariant, loading, children })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub files for hooks, API helpers, and components</name>
  <files>
    dashboard/src/hooks/useProjectSettings.js,
    dashboard/src/hooks/usePromptConfigs.js,
    dashboard/src/lib/settingsApi.js,
    dashboard/src/components/settings/ConfigTab.jsx,
    dashboard/src/components/settings/PromptsTab.jsx,
    dashboard/src/components/settings/PromptCard.jsx
  </files>
  <action>
Create 6 stub files so all import paths resolve for tests:

1. `dashboard/src/hooks/useProjectSettings.js` — export `useProjectSettings(projectId)` returning `{ data: null, isLoading: true, error: null }` and `useUpdateSettings(projectId)` returning `useMutation` stub with `mutate: () => {}`.

2. `dashboard/src/hooks/usePromptConfigs.js` — export `usePromptConfigs(projectId)` returning `{ data: [], isLoading: true, error: null }` and `usePromptMutations(projectId)` returning `{ updatePrompt: { mutate: () => {} }, revertPrompt: { mutate: () => {} }, regenerateAll: { mutate: () => {} } }`.

3. `dashboard/src/lib/settingsApi.js` — export `updateProjectSettings(projectId, fields)`, `updatePrompt(promptId, promptText)`, `revertPrompt(promptId, version)`, `regenerateAllPrompts(projectId)` — all calling `webhookCall()` from `./api`.

4. `dashboard/src/components/settings/ConfigTab.jsx` — default export functional component rendering `<div data-testid="config-tab">ConfigTab</div>`. Props: `{ projectId }`.

5. `dashboard/src/components/settings/PromptsTab.jsx` — default export rendering `<div data-testid="prompts-tab">PromptsTab</div>`. Props: `{ projectId }`.

6. `dashboard/src/components/settings/PromptCard.jsx` — default export rendering `<div data-testid="prompt-card">PromptCard</div>`. Props: `{ prompt, projectId, onSave }`.

Create `dashboard/src/components/settings/` directory first.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && node -e "import('./src/hooks/useProjectSettings.js').then(m => console.log('useProjectSettings:', typeof m.useProjectSettings)).catch(e => {console.error(e.message); process.exit(1)})" 2>/dev/null || echo "Stub imports verified via file existence" && ls src/hooks/useProjectSettings.js src/hooks/usePromptConfigs.js src/lib/settingsApi.js src/components/settings/ConfigTab.jsx src/components/settings/PromptsTab.jsx src/components/settings/PromptCard.jsx</automated>
  </verify>
  <done>All 6 stub files exist with correct exports, creating importable contracts for tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Settings test scaffolds covering OPS-03 and OPS-04</name>
  <files>dashboard/src/__tests__/Settings.test.jsx</files>
  <behavior>
    - Test: Settings page renders with two tabs (Configuration, Prompts)
    - Test: Configuration tab shows Production Config section with editable fields
    - Test: Save button appears when a config field is edited
    - Test: YouTube/Drive section shows editable fields with independent save
    - Test: Read-only sections (API, Security, Appearance) render without edit buttons
    - Test: Prompts tab renders 7 prompt cards in 3 category groups
    - Test: Prompt card expands on click showing textarea
    - Test: Version badge click shows version history dropdown
    - Test: Regenerate All button shows confirmation dialog
  </behavior>
  <action>
Create `dashboard/src/__tests__/Settings.test.jsx` with 9 test cases (all expected to FAIL since components are stubs).

Mock setup:
- `vi.mock('../hooks/useProjectSettings')` returning mock data with production config fields (script_approach: '3_pass', images_per_video: 100, etc.)
- `vi.mock('../hooks/usePromptConfigs')` returning 7 mock prompts with types matching the 3 groups
- `vi.mock('react-router', () => ({ useParams: () => ({ id: 'test-project-id' }) }))`
- `vi.mock('../lib/supabase')` as done in other test files

Test structure:
```javascript
describe('Settings Page', () => {
  describe('Tab Navigation', () => {
    it('renders Configuration and Prompts tabs')
  })
  describe('Configuration Tab (OPS-03)', () => {
    it('renders Production Config with editable fields')
    it('shows Save button when field is edited')
    it('renders YouTube/Drive fields with independent save')
    it('renders read-only sections without edit controls')
  })
  describe('Prompts Tab (OPS-04)', () => {
    it('renders 7 prompt cards in 3 groups')
    it('expands prompt card on click showing textarea')
    it('shows version dropdown on badge click')
    it('shows ConfirmDialog on Regenerate All click')
  })
})
```

Use `@testing-library/react` render, screen, fireEvent. Follow the same mock patterns as existing test files (e.g., `ProductionMonitor.test.jsx`).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run --reporter=verbose -- Settings.test 2>&1 | tail -20</automated>
  </verify>
  <done>Settings.test.jsx exists with 9 test cases. Tests are expected to fail (RED phase) since components are stubs. All imports resolve without errors.</done>
</task>

</tasks>

<verification>
- All 7 new files exist in correct directories
- Test file imports resolve (no "module not found" errors)
- Tests run (even if failing) without crash
</verification>

<success_criteria>
- 6 stub files with correct exports created
- 1 test file with 9 test cases covering OPS-03 and OPS-04
- `npx vitest run -- Settings.test` runs without import errors (tests may fail)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-00-SUMMARY.md`
</output>
