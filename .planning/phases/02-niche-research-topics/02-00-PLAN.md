---
phase: 02-niche-research-topics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/src/__tests__/CreateProjectModal.test.jsx
  - dashboard/src/__tests__/ProjectsHome.test.jsx
  - dashboard/src/__tests__/TopicReview.test.jsx
  - dashboard/src/__tests__/TopicActions.test.jsx
  - dashboard/src/__tests__/TopicBulkActions.test.jsx
autonomous: true
requirements:
  - NICH-01
  - NICH-07
  - TOPC-05
  - TOPC-06
  - TOPC-07
  - TOPC-08
  - TOPC-10

must_haves:
  truths:
    - "Vitest runs and discovers all 5 test files"
    - "All tests fail with meaningful assertion errors (RED phase)"
    - "Test scaffolds describe the behavioral contracts plans 01-04 must satisfy"
  artifacts:
    - path: "dashboard/src/__tests__/CreateProjectModal.test.jsx"
      provides: "Stub tests for NICH-01: modal open, validation, submit"
    - path: "dashboard/src/__tests__/ProjectsHome.test.jsx"
      provides: "Stub tests for NICH-07: renders projects, empty state, create button"
    - path: "dashboard/src/__tests__/TopicReview.test.jsx"
      provides: "Stub tests for TOPC-05: renders topic cards, grouping, filters"
    - path: "dashboard/src/__tests__/TopicActions.test.jsx"
      provides: "Stub tests for TOPC-06/07/08: approve, reject, refine actions"
    - path: "dashboard/src/__tests__/TopicBulkActions.test.jsx"
      provides: "Stub tests for TOPC-10: multi-select, bulk approve, bulk reject"
  key_links:
    - from: "dashboard/src/__tests__/CreateProjectModal.test.jsx"
      to: "dashboard/src/components/projects/CreateProjectModal.jsx"
      via: "import and render"
      pattern: "CreateProjectModal"
    - from: "dashboard/src/__tests__/TopicReview.test.jsx"
      to: "dashboard/src/pages/TopicReview.jsx"
      via: "import and render"
      pattern: "TopicReview"
---

<objective>
Create Wave 0 test scaffolds that define behavioral contracts for all Phase 2 UI components before implementation begins.

Purpose: Satisfy Nyquist compliance -- every plan task needs an automated test that confirms wiring and functionality, not just "vite build passes."
Output: 5 test files with failing stubs that will turn green as Plans 01-04 implement the components.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-niche-research-topics/02-CONTEXT.md
@.planning/phases/02-niche-research-topics/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest config + 5 test scaffold files with failing stubs</name>
  <files>
    dashboard/src/__tests__/CreateProjectModal.test.jsx,
    dashboard/src/__tests__/ProjectsHome.test.jsx,
    dashboard/src/__tests__/TopicReview.test.jsx,
    dashboard/src/__tests__/TopicActions.test.jsx,
    dashboard/src/__tests__/TopicBulkActions.test.jsx
  </files>
  <action>
Verify Vitest is already configured from Phase 1 (check vite.config.js for test block and @testing-library/react in package.json). If missing, add vitest + @testing-library/react + jsdom to devDependencies and add `test: { environment: 'jsdom', globals: true }` to vite.config.js.

Create 5 test files with descriptive failing stubs. Each test imports the component it will verify (even though it does not exist yet -- the import failure IS the red state). Use `describe` + `it` blocks with clear behavioral names.

**CreateProjectModal.test.jsx** (NICH-01):
```
- it('renders modal when isOpen is true')
- it('requires niche name to be non-empty')
- it('calls useCreateProject mutation on submit')
- it('shows success animation after submit')
```

**ProjectsHome.test.jsx** (NICH-07):
```
- it('renders project cards from useProjects data')
- it('shows empty state when no projects exist')
- it('opens CreateProjectModal on New Project click')
- it('shows skeleton cards while loading')
```

**TopicReview.test.jsx** (TOPC-05):
```
- it('renders topic cards grouped by playlist_angle')
- it('filters topics by status dropdown')
- it('filters topics by playlist dropdown')
- it('shows skeleton cards during generation')
- it('shows summary bar with correct counts')
```

**TopicActions.test.jsx** (TOPC-06, TOPC-07, TOPC-08):
```
- it('approve button sets review_status to approved')  (TOPC-06)
- it('reject button opens confirmation with optional feedback')  (TOPC-07)
- it('refine button opens side panel with instructions textarea')  (TOPC-08)
- it('edit button opens side panel with editable fields')
```

**TopicBulkActions.test.jsx** (TOPC-10):
```
- it('shows bulk bar when topics are selected')
- it('bulk approve calls mutation with all selected IDs')
- it('bulk reject calls mutation with all selected IDs')
- it('clear selection hides bulk bar')
```

Each test body should have a placeholder assertion: `expect(true).toBe(false) // RED: implement in plan XX-YY`. This ensures all tests are discovered and fail, confirming the RED state.

Mock react-router useParams to return a projectId. Mock supabase client. Mock TanStack Query hooks where needed for rendering tests.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files are discovered by Vitest. All tests fail (RED state). No test passes prematurely. Vitest exits with non-zero code and lists all failing test names.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run --reporter=verbose` discovers all 5 files
- All tests fail with descriptive names
- No import errors crash the test runner (mocks handle missing components)
</verification>

<success_criteria>
Vitest discovers and runs all 5 test files. Every test fails with a placeholder assertion. The test names clearly describe the behavioral contracts that Plans 01-04 must satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/02-niche-research-topics/02-00-SUMMARY.md`
</output>
