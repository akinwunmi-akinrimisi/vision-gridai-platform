---
phase: 02-niche-research-topics
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - dashboard/src/pages/NicheResearch.jsx
  - dashboard/src/components/research/BlueOceanHero.jsx
  - dashboard/src/components/research/CompetitorCards.jsx
  - dashboard/src/components/research/PainPoints.jsx
  - dashboard/src/components/research/KeywordCloud.jsx
  - dashboard/src/components/research/PlaylistCards.jsx
  - dashboard/src/components/research/RedOceanList.jsx
autonomous: true
requirements:
  - NICH-03
  - NICH-06
  - NICH-07

must_haves:
  truths:
    - "User can navigate to /project/:id/research and see niche research results"
    - "Blue-ocean strategy is the hero section -- most visually prominent element"
    - "Competitor audit displays as channel cards with key metrics"
    - "Pain points grouped by source (Reddit, Quora, forums)"
    - "Keywords shown as colored tag cloud"
    - "3 playlist angles shown as editable cards"
    - "Generate Topics button redirects to /project/:id/topics"
  artifacts:
    - path: "dashboard/src/pages/NicheResearch.jsx"
      provides: "Two-column research page with all sections"
    - path: "dashboard/src/components/research/BlueOceanHero.jsx"
      provides: "Hero section with channel positioning + value curve gaps"
    - path: "dashboard/src/components/research/CompetitorCards.jsx"
      provides: "Channel cards with name, subs, views, coverage tags"
    - path: "dashboard/src/components/research/PlaylistCards.jsx"
      provides: "Editable playlist angle cards with regenerate option"
  key_links:
    - from: "dashboard/src/pages/NicheResearch.jsx"
      to: "useNicheProfile + useProject hooks"
      via: "TanStack Query data fetching"
      pattern: "useNicheProfile|useProject"
    - from: "dashboard/src/pages/NicheResearch.jsx"
      to: "/project/:id/topics"
      via: "navigate() on Generate Topics click"
      pattern: "navigate.*topics"
---

<objective>
Build the Niche Research page that displays all research results (competitor audit, pain points, keywords, blue-ocean analysis, playlist angles) after a project is researched.

Purpose: Users need to review the AI-generated niche research before generating topics. This is the intelligence layer that informs the entire channel strategy.
Output: Fully functional /project/:id/research page with 6 sub-components displaying research JSONB data from niche_profiles and projects tables.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-niche-research-topics/02-CONTEXT.md
@.planning/phases/02-niche-research-topics/02-RESEARCH.md
@.planning/phases/02-niche-research-topics/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 -->

From dashboard/src/hooks/useNicheProfile.js:
```javascript
export function useNicheProfile(projectId) // { data: NicheProfile, isLoading, error }
export function useProject(projectId) // { data: Project, isLoading, error }
```

NicheProfile shape (from niche_profiles table):
```
{
  competitor_analysis: { channels: [...], top_videos: [...], gaps: [...] },
  audience_pain_points: { reddit: [...], quora: [...], forums: [...] },
  keyword_research: { high_volume: [...], low_competition: [...], trending: [...] },
  blue_ocean_opportunities: { unoccupied_angles: [...], value_curve_gaps: [...] }
}
```

Project shape (relevant fields):
```
{
  playlist1_name, playlist1_theme,
  playlist2_name, playlist2_theme,
  playlist3_name, playlist3_theme,
  niche_blue_ocean_strategy,
  niche_expertise_profile,
  niche_red_ocean_topics: string[],
  status
}
```

From dashboard/src/components/ui/ConfirmDialog.jsx:
```javascript
export default function ConfirmDialog({ isOpen, onClose, onConfirm, title, message, confirmText, confirmVariant, loading })
```

From dashboard/src/lib/api.js:
```javascript
export const generateTopics = (projectId) => webhookCall('topics/generate', { project_id: projectId })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NicheResearch page + research sub-components</name>
  <files>
    dashboard/src/pages/NicheResearch.jsx,
    dashboard/src/components/research/BlueOceanHero.jsx,
    dashboard/src/components/research/CompetitorCards.jsx,
    dashboard/src/components/research/PainPoints.jsx,
    dashboard/src/components/research/KeywordCloud.jsx,
    dashboard/src/components/research/PlaylistCards.jsx,
    dashboard/src/components/research/RedOceanList.jsx
  </files>
  <action>
**NicheResearch.jsx** -- Main page component:
- Get projectId from useParams (import from 'react-router')
- Use useProject(projectId) and useNicheProfile(projectId) hooks
- Loading state: full-page skeleton with shimmer blocks mimicking two-column layout
- Error state: glass-card with error message + "Retry" button
- If project status is still 'researching*': show "Research in progress..." with animated spinner and step indicator (reuse research progress pattern from ProjectCard)
- **Two-column grid layout** per user decision: `grid grid-cols-1 lg:grid-cols-2 gap-6`
  - Left column: BlueOceanHero (top, spans full width of left col) -> PlaylistCards -> "Generate Topics" CTA button (only when status='ready_for_topics')
  - Right column: CompetitorCards -> PainPoints -> KeywordCloud -> RedOceanList
- **Summary dashboard view**: Each section has a collapsed summary by default with "View Full Research" expand toggle per user decision
- "Generate Topics" button: calls generateTopics(projectId) via webhookCall, then navigates to `/project/${projectId}/topics` immediately
- "Re-research Niche" button with ConfirmDialog: "This will overwrite existing research. Continue?" Calls webhookCall('project/research', { project_id: projectId })
- Page header: project name + niche badge + status badge

**BlueOceanHero.jsx** -- Props: { strategy, expertiseProfile, valueGaps }
- Per user decision: "most prominent element" -- gradient border (blue-500 to purple-500) or accent bg-blue-500/5 dark:bg-blue-500/10
- Channel positioning statement in large text
- Value curve gaps as bullet points
- Expertise profile shown as a subtitle/tagline
- glass-card with elevated styling

**CompetitorCards.jsx** -- Props: { competitors }
- Grid of channel cards (2 cols on lg)
- Each card: channel name, subscriber count, avg views, coverage tags (badge-blue), gap tags (badge-green)
- Collapsed: show top 3-5 channels. Expand to see all.

**PainPoints.jsx** -- Props: { painPoints }
- Grouped by source: Reddit, Quora, Forums sections
- Source header with icon (MessageCircle for Reddit, HelpCircle for Quora, Users for Forums)
- Each pain point as a quoted block with slight indent and border-left-2 border-blue-500
- Collapsed: show top 3 per source. Expand for all.

**KeywordCloud.jsx** -- Props: { keywords }
- Colored tag cloud per user decision: high_volume=badge-blue, low_competition=badge-green, trending=badge-amber
- Tags displayed as flex-wrap with gap-2
- Each tag shows keyword text

**PlaylistCards.jsx** -- Props: { playlists, onRegenerate }
- 3 cards (one per playlist angle) in a row
- Each shows: playlist number, name, theme description
- "Regenerate Playlists" button below (calls webhookCall to re-research playlists)
- Per user decision: "editable cards" -- clicking makes name/theme fields editable inline (simple contentEditable or input toggle)

**RedOceanList.jsx** -- Props: { topics }
- Per user decision: "warning list with red/amber styling"
- Header: "Topics to Avoid (Oversaturated)" with AlertTriangle icon
- List of topics as badge-red tags or red-tinted list items
- glass-card with border-red-500/20 accent

All components: dark mode support, glass-card styling, lucide-react icons, responsive. Handle null/undefined data gracefully (show "No data yet" message).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vite build 2>&1 | tail -5 && grep -c "useNicheProfile\|useProject" src/pages/NicheResearch.jsx && grep "navigate" src/pages/NicheResearch.jsx | grep -c "topics" && grep -l "BlueOceanHero\|CompetitorCards\|PainPoints\|KeywordCloud\|PlaylistCards\|RedOceanList" src/pages/NicheResearch.jsx && echo "NicheResearch wiring verified"</automated>
  </verify>
  <done>NicheResearch page renders two-column layout with all 6 research sub-components, each handling real JSONB data with collapse/expand, blue-ocean hero is visually prominent, Generate Topics button navigates to topics page, Re-research button shows confirmation dialog</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vite build` succeeds
- Navigating to /project/:id/research renders the research page
- All 6 sub-components handle both populated and empty data states
- Generate Topics button navigates to /project/:id/topics
- Re-research button shows confirmation dialog
</verification>

<success_criteria>
User can navigate to the Research page after project research completes, see all niche research data organized in a two-column layout with blue-ocean hero as the most prominent element, review competitors/pain points/keywords/playlists, and click Generate Topics to proceed to topic generation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-niche-research-topics/02-03-SUMMARY.md`
</output>
