---
phase: 04-production-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/src/__tests__/ProductionMonitor.test.jsx
  - dashboard/src/__tests__/DotGrid.test.jsx
  - dashboard/src/__tests__/HeroCard.test.jsx
  - dashboard/src/__tests__/FailedScenes.test.jsx
  - dashboard/src/__tests__/PipelineTable.test.jsx
  - dashboard/src/__tests__/productionApi.test.js
  - dashboard/src/__tests__/useProductionProgress.test.js
  - dashboard/src/__tests__/SupervisorAlert.test.jsx
  - dashboard/src/hooks/useProductionProgress.js
  - dashboard/src/hooks/useProductionMutations.js
  - dashboard/src/hooks/useProductionLog.js
  - dashboard/src/hooks/useProjectMetrics.js
  - dashboard/src/components/production/HeroCard.jsx
  - dashboard/src/components/production/DotGrid.jsx
  - dashboard/src/components/production/QueueList.jsx
  - dashboard/src/components/production/FailedScenes.jsx
  - dashboard/src/components/production/ActivityLog.jsx
  - dashboard/src/components/production/CostEstimateDialog.jsx
  - dashboard/src/components/production/SupervisorAlert.jsx
  - dashboard/src/components/dashboard/PipelineTable.jsx
autonomous: true
requirements: [PROD-13, PROD-14, OPS-01]

must_haves:
  truths:
    - "8 test files exist with failing tests defining Production Monitor and PipelineTable behaviors"
    - "Stub files exist for all new hooks and components so Vite import analysis resolves"
    - "Existing Phase 3 tests remain unaffected"
  artifacts:
    - path: "dashboard/src/__tests__/ProductionMonitor.test.jsx"
      provides: "Page-level rendering tests"
    - path: "dashboard/src/__tests__/DotGrid.test.jsx"
      provides: "Dot grid chapter grouping and color tests"
    - path: "dashboard/src/__tests__/HeroCard.test.jsx"
      provides: "Hero card stage chips and ETA tests"
    - path: "dashboard/src/__tests__/FailedScenes.test.jsx"
      provides: "Failed scene retry/skip action tests"
    - path: "dashboard/src/__tests__/PipelineTable.test.jsx"
      provides: "Pipeline table topic row rendering tests"
    - path: "dashboard/src/__tests__/productionApi.test.js"
      provides: "Production webhook API helper tests"
    - path: "dashboard/src/__tests__/useProductionProgress.test.js"
      provides: "Production progress hook + Realtime tests"
    - path: "dashboard/src/__tests__/SupervisorAlert.test.jsx"
      provides: "Supervisor alert banner tests"
  key_links:
    - from: "test files"
      to: "stub files"
      via: "ES module imports"
      pattern: "import.*from.*components/production"
---

<objective>
Wave 0: Create test scaffolds and stub files for all Phase 4 dashboard components, hooks, and API helpers.

Purpose: Establish RED-phase TDD tests that define expected behavior before implementation. Stubs ensure Vite import analysis resolves during test runs.
Output: 8 failing test files + 11 stub files (4 hooks, 7 components)
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-production-pipeline/04-CONTEXT.md
@.planning/phases/04-production-pipeline/04-RESEARCH.md

@dashboard/src/__tests__/setup.js
@dashboard/src/hooks/useRealtimeSubscription.js
@dashboard/src/hooks/useTopics.js
@dashboard/src/lib/api.js

<interfaces>
<!-- Existing patterns from Phase 2/3 Wave 0 stubs -->
<!-- Stubs export functions that throw 'not implemented' so Vite resolves imports -->

From dashboard/src/lib/api.js:
```javascript
export async function webhookCall(endpoint, data = {}) { ... }
export const createProject = (data) => webhookCall('project/create', data);
export const generateTopics = (projectId) => webhookCall('topics/generate', { project_id: projectId });
export const generateScript = (topicId) => webhookCall('script/generate', { topic_id: topicId });
```

From dashboard/src/hooks/useRealtimeSubscription.js:
```javascript
export function useRealtimeSubscription(table, filter, queryKeys) { ... }
```

From dashboard/src/hooks/useTopics.js:
```javascript
export function useTopics(projectId) { ... }
export function useApproveTopics(projectId) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for Production Monitor components + API helpers</name>
  <files>
    dashboard/src/__tests__/ProductionMonitor.test.jsx
    dashboard/src/__tests__/DotGrid.test.jsx
    dashboard/src/__tests__/HeroCard.test.jsx
    dashboard/src/__tests__/FailedScenes.test.jsx
    dashboard/src/__tests__/productionApi.test.js
    dashboard/src/__tests__/useProductionProgress.test.js
    dashboard/src/__tests__/SupervisorAlert.test.jsx
  </files>
  <action>
Create 7 test files following the exact patterns established in Phase 2/3 Wave 0 (see ScriptReview.test.jsx and TopicReview.test.jsx for reference patterns). Mock supabase, react-router, and api modules using vi.mock at the top of each file.

**ProductionMonitor.test.jsx** (~12 tests):
- Renders "No active production" empty state when no topic has status='producing'
- Empty state shows count of script-approved topics ready for production
- Empty state shows "Start Production" CTA button
- Renders HeroCard when active topic exists (status='producing')
- Renders DotGrid with scenes for active topic
- Renders QueueList with topics having status='queued'
- Renders FailedScenes section when any scene has failed status
- Does NOT render FailedScenes when no failures exist
- Renders ActivityLog (collapsed by default)
- Renders "Recently Completed" section with last 3 completed topics
- Renders supervisor alert banner when any topic has supervisor_alerted=true
- Stop Production button visible on active topic

**DotGrid.test.jsx** (~8 tests):
- Groups scenes by chapter with chapter name labels
- Colors dot gray for pending scenes (all statuses 'pending')
- Colors dot blue for audio-complete scenes (audio_status='uploaded')
- Colors dot cyan for image-complete scenes (image_status='uploaded')
- Colors dot purple for video-complete scenes (video_status='uploaded')
- Colors dot green for fully complete scenes (clip_status='complete')
- Colors dot red for failed scenes
- Shows tooltip on hover with scene number, chapter, and per-asset status breakdown

**HeroCard.test.jsx** (~8 tests):
- Displays active topic title and topic number
- Shows 5 stage chips: Audio, Images, I2V, T2V, Assembly
- Completed stage chip shows checkmark
- Active stage chip has pulse/glow animation class
- Pending stage chips are dimmed
- Shows elapsed time counter
- Shows rolling-average ETA
- Shows live cost counter with per-stage breakdown

**FailedScenes.test.jsx** (~7 tests):
- Lists failed scenes with scene number and user-friendly error summary
- Shows expandable raw error details (API name, error code, prompt text, attempt count)
- Per-scene Retry button calls retryScene API
- Per-scene Skip button calls skipScene API
- "Edit & Retry" button opens image prompt textarea
- "Retry All Failed" batch button calls retryAllFailed API
- "Skip All Failed" batch button calls skipAllFailed API

**productionApi.test.js** (~12 tests):
- triggerProduction calls webhookCall('production/trigger', { topic_id })
- triggerProductionBatch calls webhookCall('production/trigger-batch', { topic_ids })
- stopProduction calls webhookCall('production/stop', { topic_id })
- resumeProduction calls webhookCall('production/resume', { topic_id })
- restartProduction calls webhookCall('production/restart', { topic_id })
- retryScene calls webhookCall('production/retry-scene', { scene_id })
- retryAllFailed calls webhookCall('production/retry-all-failed', { topic_id })
- skipScene calls webhookCall('production/skip-scene', { scene_id, reason })
- skipAllFailed calls webhookCall('production/skip-all-failed', { topic_id })
- editAndRetryScene calls webhookCall('production/edit-retry-scene', { scene_id, image_prompt })
- assembleVideo calls webhookCall('production/assemble', { topic_id })
- All functions return webhook response format

**useProductionProgress.test.js** (~6 tests):
- Fetches scenes for given topicId from Supabase ordered by scene_number
- Returns empty array when topicId is null
- Subscribes to Realtime on scenes table filtered by topic_id
- Unsubscribes on unmount
- Computes stage completion counts from scene statuses
- Returns isLoading/error states

**SupervisorAlert.test.jsx** (~4 tests):
- Renders amber warning banner when supervisor_alerted=true
- Does not render when supervisor_alerted=false
- Shows "Supervisor detected stuck pipeline" message
- Dismiss button resets alert state

Use mock data structures matching the Supabase schema: scenes with audio_status, image_status, video_status, clip_status, chapter, scene_number, narration_text, audio_duration_ms, start_time_ms, end_time_ms, skipped, image_prompt. Topics with status, supervisor_alerted, total_cost, cost_breakdown, last_status_change.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/ProductionMonitor.test.jsx src/__tests__/DotGrid.test.jsx src/__tests__/HeroCard.test.jsx src/__tests__/FailedScenes.test.jsx src/__tests__/productionApi.test.js src/__tests__/useProductionProgress.test.js src/__tests__/SupervisorAlert.test.jsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>7 test files exist, all tests run and fail (RED phase), no import resolution errors</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffold for PipelineTable + all stub files</name>
  <files>
    dashboard/src/__tests__/PipelineTable.test.jsx
    dashboard/src/hooks/useProductionProgress.js
    dashboard/src/hooks/useProductionMutations.js
    dashboard/src/hooks/useProductionLog.js
    dashboard/src/hooks/useProjectMetrics.js
    dashboard/src/components/production/HeroCard.jsx
    dashboard/src/components/production/DotGrid.jsx
    dashboard/src/components/production/QueueList.jsx
    dashboard/src/components/production/FailedScenes.jsx
    dashboard/src/components/production/ActivityLog.jsx
    dashboard/src/components/production/CostEstimateDialog.jsx
    dashboard/src/components/production/SupervisorAlert.jsx
    dashboard/src/components/dashboard/PipelineTable.jsx
  </files>
  <action>
Create 1 test file + 11 stub files following the established Phase 3 stub pattern (exports that throw 'not implemented' on call, components that render null).

**PipelineTable.test.jsx** (~6 tests):
- Renders table with topic rows showing topic #, title, playlist angle
- Shows color-coded status badge per topic (Pending gray, Approved blue, Scripting cyan, Script Approved green, Producing amber, Assembly purple, Assembled blue, Published green, Failed red)
- Shows progress percentage for producing topics
- Shows quality score for scripted topics
- Shows views and revenue columns (0 when not published)
- Subscribes to Realtime for live topic status updates

**Stub files (hooks):** Create `dashboard/src/hooks/useProductionProgress.js`, `useProductionMutations.js`, `useProductionLog.js`, `useProjectMetrics.js`. Each exports named functions that throw `new Error('Not implemented - stub')`. Follow the useScriptMutations.js stub pattern.

Hook exports:
- useProductionProgress(topicId) -> { scenes, stageProgress, isLoading, error }
- useProductionMutations(projectId) -> { triggerProduction, stopProduction, resumeProduction, restartProduction, retryScene, skipScene, editAndRetryScene, retryAllFailed, skipAllFailed }
- useProductionLog(topicId) -> { logs, isLoading }
- useProjectMetrics(projectId) -> { metrics, isLoading }

**Stub files (components):** Create `dashboard/src/components/production/` directory with HeroCard.jsx, DotGrid.jsx, QueueList.jsx, FailedScenes.jsx, ActivityLog.jsx, CostEstimateDialog.jsx, SupervisorAlert.jsx. Also create `dashboard/src/components/dashboard/PipelineTable.jsx`. Each renders `null` (same pattern as Phase 3 ScorePanel.jsx stub).

Verify existing tests still pass after adding stubs (no accidental import breakage).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>8 total test files exist with all new tests failing (RED), 11 stub files exist with correct exports, all pre-existing tests pass</done>
</task>

</tasks>

<verification>
- All 8 new test files load without import errors
- All new tests fail (RED phase expected)
- All pre-existing Phase 1/2/3 tests pass unchanged
- Stub files export the correct function/component signatures
</verification>

<success_criteria>
- 8 test files with ~63 total test cases, all failing as RED
- 11 stub files with throw-on-call exports
- 0 import resolution errors from Vite
- Pre-existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-pipeline/04-00-SUMMARY.md`
</output>
