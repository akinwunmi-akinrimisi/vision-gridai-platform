---
phase: 04-production-pipeline
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - dashboard/src/components/dashboard/PipelineTable.jsx
  - dashboard/src/pages/ProjectDashboard.jsx
  - dashboard/src/components/topics/TopicCard.jsx
autonomous: true
requirements: [PROD-14]

must_haves:
  truths:
    - "Project Dashboard shows a live-updating pipeline status table with all topics"
    - "Pipeline table shows topic #, title, playlist angle, status badge, progress %, score, views, revenue"
    - "TopicCard shows production status badge and mini progress bar for producing topics"
  artifacts:
    - path: "dashboard/src/components/dashboard/PipelineTable.jsx"
      provides: "Live pipeline status table component"
      min_lines: 60
    - path: "dashboard/src/pages/ProjectDashboard.jsx"
      provides: "Rewritten Project Dashboard with real metrics + pipeline table"
      min_lines: 80
  key_links:
    - from: "dashboard/src/pages/ProjectDashboard.jsx"
      to: "dashboard/src/hooks/useTopics.js"
      via: "hook import for live topic data"
      pattern: "useTopics"
    - from: "dashboard/src/pages/ProjectDashboard.jsx"
      to: "dashboard/src/hooks/useProjectMetrics.js"
      via: "hook import for aggregated metrics"
      pattern: "useProjectMetrics"
---

<objective>
Build the Project Dashboard pipeline table and update TopicCard with production status indicators.

Purpose: PROD-14 requires a live pipeline status table on the Project Dashboard showing all topics with real-time status and progress. TopicCard needs production badges for the Topic Review page.
Output: PipelineTable component, rewritten ProjectDashboard, updated TopicCard
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-production-pipeline/04-CONTEXT.md
@.planning/phases/04-production-pipeline/04-01-SUMMARY.md

@dashboard/src/pages/ProjectDashboard.jsx
@dashboard/src/components/topics/TopicCard.jsx
@dashboard/src/hooks/useTopics.js
@dashboard/src/hooks/useProjectMetrics.js

<interfaces>
From dashboard/src/hooks/useProjectMetrics.js (from Plan 04-01):
```javascript
export function useProjectMetrics(projectId) {
  // Returns: { metrics: { totalTopics, approved, inProgress, published, failed,
  //            totalSpend, totalRevenue, avgCpm }, isLoading }
}
```

From dashboard/src/hooks/useTopics.js:
```javascript
export function useTopics(projectId) {
  // Returns: { data: topics[], isLoading, error }
  // topic shape: { id, topic_number, seo_title, playlist_angle, status,
  //   script_quality_score, yt_views, yt_estimated_revenue, total_cost,
  //   audio_progress, images_progress, i2v_progress, t2v_progress, assembly_status }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PipelineTable component + rewrite ProjectDashboard page</name>
  <files>
    dashboard/src/components/dashboard/PipelineTable.jsx
    dashboard/src/pages/ProjectDashboard.jsx
  </files>
  <behavior>
    - PipelineTable renders topic rows with #, title, angle, status badge, progress %, score, views, revenue
    - Status badges color-coded per CONTEXT.md decision
    - Progress bar for producing topics
    - ProjectDashboard shows real metrics from useProjectMetrics, pipeline table from useTopics
  </behavior>
  <action>
**PipelineTable.jsx** (props: topics):
- Responsive table inside glass-card with overflow-x-auto
- Header: "Pipeline Status" + subtitle "All topics and their production progress"
- Table columns: #, Title (truncated 40 chars), Angle, Status, Progress, Score, Views, Revenue
- Status badges using existing badge-* CSS classes, color-coded per CONTEXT.md:
  - Pending: badge (default gray)
  - Approved: badge-blue
  - Scripting: badge-cyan (custom bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30 dark:text-cyan-300)
  - Script Approved: badge-emerald
  - Queued: badge-amber
  - Producing: badge-amber + animate-pulse
  - Audio/Images/Assembling: badge-purple
  - Assembled: badge-blue
  - Published: badge-emerald
  - Failed: badge-red
- Progress column: mini progress bar (h-1.5 rounded-full) for producing/audio/images/assembling topics. Compute from audio_progress, images_progress, etc. Show "100%" for assembled/published.
- Score column: topic.script_quality_score or "--"
- Views column: topic.yt_views formatted with commas, or "--"
- Revenue column: "$" + topic.yt_estimated_revenue formatted, or "--"
- Empty state: "Pipeline table will populate when topics are generated"
- Mobile: stack to card layout below md breakpoint

**ProjectDashboard.jsx** full rewrite (currently skeleton with hardcoded metrics):
- Use useParams() for projectId, useTopics(projectId), useProjectMetrics(projectId)
- Top metrics grid (4 cards): Total Topics, Approved, In Progress, Published -- with real data from metrics
- Financial metrics grid (4 cards): Total Spend ($), Revenue ($), ROI (revenue/spend or "--"), Avg CPM
- PipelineTable component with topics data
- Remove ComingSoon placeholder
- Loading state: SkeletonLoader cards while data fetches

Run PipelineTable tests to verify GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/PipelineTable.test.jsx --reporter=verbose</automated>
  </verify>
  <done>PipelineTable tests pass. ProjectDashboard renders real metrics and pipeline table. No hardcoded data.</done>
</task>

<task type="auto">
  <name>Task 2: Update TopicCard with production status badges and mini progress bar</name>
  <files>dashboard/src/components/topics/TopicCard.jsx</files>
  <action>
Extend the existing TopicCard component (already has script status badges from Phase 3) with production-specific status display.

Add to TopicCard:
1. **Production status badge:** When topic.status is one of 'queued', 'producing', 'audio', 'images', 'assembling', 'assembled', 'stopped', 'failed', show a colored badge below the existing review_status/script badges. Use same color scheme as PipelineTable.
2. **Mini progress bar:** When topic.status IN ('producing', 'audio', 'images', 'assembling'), show a tiny progress bar (h-1 rounded-full) beneath the card content. Compute progress from audio_progress, images_progress, i2v_progress, t2v_progress fields (format: "done:X/Y" or "complete").
3. **"Start Production" button:** When topic.script_review_status === 'approved' AND topic.status === 'script_approved', show a "Start Production" button (Play icon) that navigates to `/project/{projectId}/production?trigger={topicId}` using useNavigate.
4. Do NOT break existing topic review functionality (approve/reject/refine buttons).

Parse progress strings: "done:47/172" -> { done: 47, total: 172 }. "complete" -> 100%. "pending" -> 0%.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>TopicCard shows production badges, mini progress, and Start Production button. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run src/__tests__/PipelineTable.test.jsx --reporter=verbose` -- all pass
- `cd dashboard && npx vitest run --reporter=verbose` -- full suite passes (no regressions)
- `cd dashboard && npm run build` -- builds without errors
</verification>

<success_criteria>
- PipelineTable renders live topic data with color-coded status badges and progress bars
- ProjectDashboard shows real aggregated metrics and pipeline table (no hardcoded values)
- TopicCard displays production status badge, mini progress bar, and Start Production button
- All topics visible in pipeline table with correct status representation
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-pipeline/04-03-SUMMARY.md`
</output>
