---
phase: 04-production-pipeline
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - dashboard/src/lib/api.js
  - dashboard/src/hooks/useProductionProgress.js
  - dashboard/src/hooks/useProductionMutations.js
  - dashboard/src/hooks/useProductionLog.js
  - dashboard/src/hooks/useProjectMetrics.js
autonomous: true
requirements: [PROD-10, PROD-12, PROD-13, PROD-14]

must_haves:
  truths:
    - "Production webhook API helpers exist and call correct endpoints"
    - "useProductionProgress hook fetches scenes for a topic and computes stage completion"
    - "useProductionMutations hook provides optimistic mutation hooks for all production actions"
    - "useProductionLog hook fetches recent production_log entries"
    - "useProjectMetrics hook computes aggregated metrics from topics table"
  artifacts:
    - path: "dashboard/src/lib/api.js"
      provides: "11 production webhook helper functions"
      contains: "triggerProduction"
    - path: "dashboard/src/hooks/useProductionProgress.js"
      provides: "Scene-level progress tracking with Realtime"
      exports: ["useProductionProgress"]
    - path: "dashboard/src/hooks/useProductionMutations.js"
      provides: "Production mutation hooks (trigger, stop, retry, skip)"
      exports: ["useProductionMutations"]
    - path: "dashboard/src/hooks/useProductionLog.js"
      provides: "Production log fetching"
      exports: ["useProductionLog"]
    - path: "dashboard/src/hooks/useProjectMetrics.js"
      provides: "Aggregated project metrics"
      exports: ["useProjectMetrics"]
  key_links:
    - from: "dashboard/src/hooks/useProductionMutations.js"
      to: "dashboard/src/lib/api.js"
      via: "imported webhook helpers"
      pattern: "import.*from.*lib/api"
    - from: "dashboard/src/hooks/useProductionProgress.js"
      to: "supabase scenes table"
      via: "useRealtimeSubscription + useQuery"
      pattern: "useRealtimeSubscription.*scenes"
---

<objective>
Build the dashboard data layer for production: API helpers, data hooks, and mutation hooks.

Purpose: All dashboard components in Plans 02-03 depend on these hooks and API helpers. This plan implements the data contracts that Wave 0 tests defined.
Output: 5 files providing the complete data interface for production UI
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-production-pipeline/04-CONTEXT.md
@.planning/phases/04-production-pipeline/04-RESEARCH.md

@dashboard/src/lib/api.js
@dashboard/src/hooks/useRealtimeSubscription.js
@dashboard/src/hooks/useTopics.js
@dashboard/src/hooks/useScriptMutations.js
@dashboard/src/lib/supabase.js

<interfaces>
<!-- Existing hook patterns to follow exactly -->

From dashboard/src/hooks/useTopics.js:
```javascript
// Pattern: useQuery + useRealtimeSubscription for live data
export function useTopics(projectId) {
  useRealtimeSubscription(
    projectId ? 'topics' : null,
    projectId ? `project_id=eq.${projectId}` : null,
    [['topics', projectId]]
  );
  return useQuery({ queryKey: ['topics', projectId], queryFn: async () => { ... }, enabled: !!projectId });
}

// Pattern: useMutation with optimistic updates
export function useApproveTopics(projectId) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ topic_ids }) => webhookCall('topics/approve', { topic_ids }),
    onMutate: async ({ topic_ids }) => { /* cancel, snapshot, optimistic set */ },
    onError: (_err, _vars, context) => { /* rollback */ },
    onSettled: () => { queryClient.invalidateQueries({ queryKey: ['topics', projectId] }); },
  });
}
```

From dashboard/src/lib/api.js:
```javascript
export async function webhookCall(endpoint, data = {}) { ... }
// Pattern: export const helperName = (args) => webhookCall('endpoint', { args });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add production webhook API helpers to api.js</name>
  <files>dashboard/src/lib/api.js</files>
  <behavior>
    - triggerProduction(topicId) calls webhookCall('production/trigger', { topic_id: topicId })
    - triggerProductionBatch(topicIds) calls webhookCall('production/trigger-batch', { topic_ids: topicIds })
    - stopProduction(topicId) calls webhookCall('production/stop', { topic_id: topicId })
    - resumeProduction(topicId) calls webhookCall('production/resume', { topic_id: topicId })
    - restartProduction(topicId) calls webhookCall('production/restart', { topic_id: topicId })
    - retryScene(sceneId) calls webhookCall('production/retry-scene', { scene_id: sceneId })
    - retryAllFailed(topicId) calls webhookCall('production/retry-all-failed', { topic_id: topicId })
    - skipScene(sceneId, reason) calls webhookCall('production/skip-scene', { scene_id: sceneId, reason })
    - skipAllFailed(topicId) calls webhookCall('production/skip-all-failed', { topic_id: topicId })
    - editAndRetryScene(sceneId, imagePrompt) calls webhookCall('production/edit-retry-scene', { scene_id: sceneId, image_prompt: imagePrompt })
    - assembleVideo(topicId) calls webhookCall('production/assemble', { topic_id: topicId })
  </behavior>
  <action>
Add 11 production webhook helper exports to the bottom of `dashboard/src/lib/api.js`. Follow the exact same pattern as existing helpers (createProject, generateScript, etc). Each is a one-liner arrow function that delegates to webhookCall with the correct endpoint and payload shape. Do NOT modify existing exports.

Run the productionApi.test.js tests to verify all 12 tests pass (GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/productionApi.test.js --reporter=verbose</automated>
  </verify>
  <done>All 12 productionApi tests pass, existing api tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement production data hooks (useProductionProgress, useProductionMutations, useProductionLog, useProjectMetrics)</name>
  <files>
    dashboard/src/hooks/useProductionProgress.js
    dashboard/src/hooks/useProductionMutations.js
    dashboard/src/hooks/useProductionLog.js
    dashboard/src/hooks/useProjectMetrics.js
  </files>
  <behavior>
    - useProductionProgress(topicId): fetches scenes WHERE topic_id=topicId ordered by scene_number, subscribes to Realtime on scenes table, computes stageProgress object { audio: { done, total }, images: { done, total }, i2v: { done, total }, t2v: { done, total }, clips: { done, total } }, returns { scenes, stageProgress, failedScenes, isLoading, error }
    - useProductionMutations(projectId): returns mutation hooks for trigger, stop, resume, restart, retryScene, skipScene, editAndRetryScene, retryAllFailed, skipAllFailed. Each uses useMutation with optimistic topic status updates and cache invalidation on ['topics', projectId] and ['scenes', topicId]
    - useProductionLog(topicId): fetches last 20 production_log entries for topicId ordered by created_at desc, subscribes to Realtime on production_log table
    - useProjectMetrics(projectId): computes from topics query: totalTopics, approved, inProgress (status IN producing/audio/images/assembling), published, totalSpend (sum total_cost), totalRevenue (sum yt_estimated_revenue)
  </behavior>
  <action>
Replace the 4 stub hook files with real implementations.

**useProductionProgress.js:**
- Use useQuery with queryKey ['scenes', topicId] to fetch from supabase.from('scenes').select('*').eq('topic_id', topicId).order('scene_number')
- Use useRealtimeSubscription('scenes', `topic_id=eq.${topicId}`, [['scenes', topicId], ['production-progress', topicId]])
- Compute stageProgress by counting scenes with audio_status='uploaded', image_status='uploaded' (for visual_type='static_image'), video_status='uploaded' (for visual_type IN 'i2v','t2v'), clip_status IN ('complete','uploaded')
- Compute failedScenes = scenes.filter(s => s.audio_status==='failed' || s.image_status==='failed' || s.video_status==='failed')
- enabled: !!topicId

**useProductionMutations.js:**
- Import all production webhook helpers from lib/api.js
- Return an object with mutation hooks, each following the useApproveTopics pattern from useTopics.js
- triggerProduction: onMutate sets topic status to 'queued', onSettled invalidates ['topics', projectId]
- stopProduction: onMutate sets topic status to 'stopped'
- retryScene/skipScene: invalidate ['scenes', topicId] on settled
- restartProduction: onMutate sets topic status to 'queued'

**useProductionLog.js:**
- useQuery with queryKey ['production-log', topicId] to fetch from supabase.from('production_log').select('*').eq('topic_id', topicId).order('created_at', { ascending: false }).limit(20)
- useRealtimeSubscription('production_log', `topic_id=eq.${topicId}`, [['production-log', topicId]])

**useProjectMetrics.js:**
- Derive metrics from the useTopics(projectId) query data (import useTopics)
- Compute counts by status, sum costs, sum revenue
- Return { metrics: { totalTopics, approved, inProgress, published, failed, totalSpend, totalRevenue, avgCpm }, isLoading }

Run all production-related tests to verify GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Documents/Antigravity/vision-gridai-platform/dashboard && npx vitest run src/__tests__/useProductionProgress.test.js src/__tests__/productionApi.test.js --reporter=verbose</automated>
  </verify>
  <done>useProductionProgress tests pass, all production API tests pass, hooks export correct shapes</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run src/__tests__/productionApi.test.js src/__tests__/useProductionProgress.test.js --reporter=verbose` -- all pass
- `cd dashboard && npx vitest run --reporter=verbose` -- full suite passes (no regressions)
</verification>

<success_criteria>
- api.js has 11 new production webhook helpers
- 4 hook files implement real data fetching with Realtime subscriptions
- useProductionProgress computes stage progress from scene statuses
- useProductionMutations provides optimistic mutations following useTopics pattern
- All Wave 0 tests for API and hooks pass (GREEN)
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-pipeline/04-01-SUMMARY.md`
</output>
