/**
 * @deprecated - Production Monitor now uses the shared HeroCard from @/components/shared/HeroCard
 * with StageProgress component. This file is kept for backward compatibility only.
 * Use the shared HeroCard + StageProgress instead.
 */
export { default } from '../shared/HeroCard';
