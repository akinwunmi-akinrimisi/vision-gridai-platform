/**
 * Configuration tab for Settings page.
 * Stub — will be implemented in Phase 06 Plan 02.
 */
export default function ConfigTab({ projectId }) {
  return <div data-testid="config-tab">ConfigTab</div>;
}
