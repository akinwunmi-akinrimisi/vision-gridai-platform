/**
 * Individual prompt card for the Prompts tab.
 * Stub — will be implemented in Phase 06 Plan 03.
 */
export default function PromptCard({ prompt, projectId, onSave }) {
  return <div data-testid="prompt-card">PromptCard</div>;
}
