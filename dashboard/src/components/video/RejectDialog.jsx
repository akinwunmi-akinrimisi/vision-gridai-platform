/**
 * RejectDialog — Gate 3 reject dialog with feedback and rollback stage selection.
 * Stub component for Phase 5 Wave 0.
 */
export default function RejectDialog({ topic, onConfirm, onClose }) {
  return (
    <div data-testid="reject-dialog">
      <p>RejectDialog placeholder</p>
    </div>
  );
}
