/**
 * MetadataPanel — Displays and edits video metadata (title, description, tags, chapters).
 * Stub component for Phase 5 Wave 0.
 */
export default function MetadataPanel({ topic, isEditing, onSave, onCancel }) {
  return (
    <div data-testid="metadata-panel">
      <p>MetadataPanel placeholder</p>
    </div>
  );
}
