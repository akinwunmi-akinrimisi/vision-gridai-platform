/**
 * PublishDialog — Gate 3 approve dialog with Publish Now / Schedule / Approve Only.
 * Stub component for Phase 5 Wave 0.
 */
export default function PublishDialog({ topic, quotaRemaining, onConfirm, onClose }) {
  return (
    <div data-testid="publish-dialog">
      <p>PublishDialog placeholder</p>
    </div>
  );
}
