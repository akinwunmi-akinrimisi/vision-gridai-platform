/**
 * BatchPublishDialog — Dialog for batch publishing multiple approved videos.
 * Stub component for Phase 5 Wave 0.
 */
export default function BatchPublishDialog({ topics, quotaRemaining, onConfirm, onClose }) {
  return (
    <div data-testid="batch-publish-dialog">
      <p>BatchPublishDialog placeholder</p>
    </div>
  );
}
